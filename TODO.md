# TODO — Settlement Operations demo

## PHASE 5 PART A — THE SNOWSIGHT RUN (updated 2026-09-10)

- ✅ 2026-09-10 — **Probe P9 received.** `SCORED_AT` is `TIMESTAMP_NTZ` with `DEFAULT CURRENT_TIMESTAMP()`. FINSERVADMIN owns the schema and both tables and holds CREATE PROCEDURE, so no grants are needed. The Snowsight session clock is America/Los_Angeles. Factor order is settled by counts; pair co-occurrence is asserted by V4c.
- ✅ 2026-09-11 — **Procedures created in Snowsight:** `simulate-reset-procedures.sql` (final version) ran to its last statement; the V10 grid read 50000 / 6416 / 12.83. Its eleven-grid verification block could not be read as evidence and is retired.
- [ ] **Run `snowflake/verification-summary.sql` and paste its ONE grid** — *Scott, blocking Part B.* Run with worksheet role FINSERVADMIN and warehouse COMPUTE_WH. Part B opens only on a grid showing all PASS; any FAIL stops everything at verbatim reporting, with no tuning. If check 24 alone fails, read its `detail` first: return-string capture inside the block is the one mechanism not proven before the run.
- [ ] **RULE: check 12's pair-count term** — *Scott, blocking 25/25.* The corrected packet (seq 10 and 12 now Large Notional 0.02 + Large Notional 0.05) has **3** distinct factor pairs, and no allowed 4th exists. `verification-summary.sql` check 12 hard-codes `COUNT(*) = 4`, so it FAILs on that term alone until authorized to read `= 3` (label "(3 pairs)"). Not changed — the file was ruled off-limits for the fix.
- [ ] **Read the Low Base Risk 0.02 + Normal Operations 0.01 baseline count in check 12's detail on the next run** — *session, on the paste.* It was truncated in the 2026-09-11 grid. It carries story 03 and nine background rows; a 0 involves a seeded story and stops for a ruling.
- ✅ 2026-09-10 — **Factor ruling applied:** packet `TOP_RISK_FACTORS` authored baseline-shaped under the coherence rule; hero-trade-spec's NULL precedent retired; V4b added to the verification block.
- ✅ 2026-09-10 — **Four rulings received and applied:** criterion High/Critical; `MATCHED_AT` trade-date evening; `SCORED_AT` call time; ≤13-character run name with exact-id reset. Region canon corrected in CLAUDE.md (NA / EMEA / APAC / LATAM).

## PHASE 5 PART B1 — SNOWFLAKE SQL API OBJECTS (2026-09-11)

- ✅ 2026-09-11 — **PAT pasted into `SO Snowflake SQL API`** — *Scott, Designer.* He also corrected the API Key header Name to `Authorization` with value `Bearer <PAT>` (the prior configuration sent a header literally named `Bearer`). Auth now passes. Original note: Authentication → API Key → value `Bearer <token>`. This HTTP connected system has no prefix field, so the value carries exactly one `Bearer `.
- ✅ 2026-09-11 — **Legacy-function rewrite RULED benign** ("report it, do not fight it"). The body was replaced by a self-built JSON string; the readback still shows `a!toJson_17r1`, judged a naming artefact. Original note: The stored `bodyContent` of `SO_simulateFeed` and `SO_resetSession` reads `a!fromJson_19r2` / `a!toJson_17r1`, where `a!fromJson` / `a!toJson` were sent. The proven JSON came from the current functions. Check what Designer shows and rule before the test.
- [ ] **Confirm "Automatically convert CDT, dictionary, or list to JSON" is now UNTICKED on both integrations** — *Scott, before the retest.* The body is a finished JSON string, and the MCP set the flag to `false`, which reads back `null`. Superseded note: "Automatically convert CDT, dictionary, or list to JSON" **ticked**; "Remove fields with null or empty values" **unticked**. The MCP set the latter to `false` and reads it back `null`.
- ✅ 2026-09-11 — **Retest passed (Scott):** `SO_simulateFeed("SMOKE")` → 200 `OK run=SMOKE trades=15 predictions=15 high_or_critical=3`; `SO_resetSession("SMOKE")` → 200 `OK run=SMOKE trades_deleted=15 predictions_deleted=15`. Environment reset.
- ✅ 2026-09-11 — **Part B2 identity RULED and applied by Scott in Designer:** SO Demo Admins viewer on `SO_simulateFeed`, `SO_resetSession` and `SO Snowflake SQL API`. No service account, no new group; scott.mcp unchanged. Readback in build-log (the integrations' grant arrives by folder inheritance).
- ✅ 2026-09-11 — **Part B2 caller contract implemented** in `SO_simulateRun` / `SO_resetRun`: message read from `body.data[1][1]`; a success with no message (202) and any integration error both surface as non-OK text, so intake never starts on them.
- [ ] **deploy-notes: record the SQL API connected system, endpoint and credential location** — *Part E.*

## RESOLVED — DEMO CONSOLE CRASH FOR A DEMO-ADMINS-ONLY IDENTITY (investigated, ruled, fixed and closed live 2026-09-24)

- ✅ 2026-09-24 — **RULED: a demo presenter is a member of BOTH `SO Demo Admins` and `SO Supervisors`, everywhere, consistently — the Demo-Admins-only reading is dead.** Implemented as a console preflight (one `a!isUserMemberOfGroup` before any query; one instruction card when it fails) and propagated to CLAUDE.md:266, CLAUDE.md:405, GETTING_STARTED and BUILD_PLAN. Original note: **is a demo presenter Demo-Admins-only, or Demo-Admins-plus-Supervisors?** *Scott.* The documentation contradicts itself — CLAUDE.md:266 "Reset/Verify and Admin page are Demo Admins only" and CLAUDE.md:405 "deliberately outside the SO Users tree" against TODO:66 "Presenter must be in SO Supervisors" and GETTING_STARTED.md "must be in both". `test.presenter` is the first identity matching the letter of "Demo Admins only" and the console crashes whole-page for it. **This one decision picks the fix and resolves the contradiction.** *Trigger: now — it blocks any SC other than Scott presenting.*
- ✅ 2026-09-24 — **DONE: Scott ran the login and GATE D closed live as `test.presenter`.** Session verified live this session — it resolves **3** pages on `settlement-ops` (including Supervisor, gated on `cons!SO_SUPERVISORS_GROUP`) and **1** on `settlement-ops-admin`, which is both memberships confirmed behaviourally rather than on report. Original note: **BLOCKED ON AN OPERATOR LOGIN: `sail login` for `test.presenter`.** *Scott.* `~/.sail-test.presenter` exists and is empty. Without it the investigation could not measure the persona render or classify the nine record-type reads as visible/empty/error — and that measurement is what discriminates the two candidate causes (degenerate value consumed unguarded vs a read that refuses rather than returns empty). One login unblocks the fix session. *Trigger: now.*
- ✅ 2026-09-24 — **FIXED in `SO_demoAdminConsole` v12** — the presenter preflight makes the whole query block unreachable for a misconfigured viewer (measured: 19 ms card branch against 613 ms configured), and line 232's consumer was guarded anyway. The "scope is larger than the line" reasoning below was right and is what produced the preflight rather than a one-line guard. Original note: **The crash itself** — `SO_demoAdminConsole` line 232, `wherecontains(true, a!forEach(items: local!agentEvents, …))`, the one query result of twenty consumed without a guard. **Scope is larger than the line even in the best case:** guarding it yields a red "Not ready · 0/9 connections" on a healthy environment, which the console's own three-state rule forbids. Either grant SO Demo Admins viewer on the nine record types, or render a reduced view that says what it cannot see. *Trigger: after the ruling above.*
- [ ] **SO Demo Admins holds no viewer right on any of the nine record types the console queries** (measured by `getObjectSecurity`, all nine grant viewer to SO Users only). Deliberate — the group sits outside the SO Users tree — but it means the nine "connection" probes can never pass for a Demo-Admins-only identity. *Trigger: part of the ruling above.*
- ✅ 2026-09-24 — **Scott added SO Demo Admins to the `SO_DemoAdmin` site's viewer role** in Designer. The site had been documented and built as "gated to SO Demo Admins" since 2026-09-09 while the **site object's** viewer list never included the group — only the page's `visibilityExpr` did. A page gate controls what you see once inside a site; it cannot let you in. Unopenable by its own audience for fifteen days.

## PHASE 6 PART 1d — FOUND 2026-09-24 (chips, downstream enforcement, agent v2)

- [ ] **The agent still occasionally points at a table the panel does not render** — 1 of 18 runs ended "The full top ten is below." *Next Ask session.* NOT a hallucination: the raw response genuinely carries a `table` element after the answer text, and the extraction drops it, so the agent is referring to something it produced and we deliberately discard. v2 forbids visual references and this slipped through anyway. Options: extend the extraction to strip a trailing sentence matching a visual reference (risky — it is answer prose), or render a compact form of the `table` element. *Trigger: before the talk track scripts the supervisor Ask beat.*
- [ ] **An answer mislabelled a denominator** — C3 run 3 said "roughly 37 times the median" where 37x is the multiple of the AVERAGE (0.51M EUR); against the median (0.16M) it is ~117x. The figures were identical across all three runs, so this is a wrong word on a right number, not unstable substance. *Trigger: watch for it in the next three-run pass; if it recurs, add "state which statistic a multiple is taken against" to the instructions.*
- [ ] **One Designer tick-box: turn off uppercase button labels.** *Scott.* `SO_SettlementOperations` → Branding → deselect **Use uppercase capitalization for button labels**. Documented as site-level only; `updateSite` exposes no branding fields over the Dev MCP, so a session cannot do it. The chip labels are already written in sentence case and will read correctly the moment it is unticked, with no code change. *Trigger: next Designer visit.*

## PHASE 6 PART 1c — FOUND 2026-09-24 (stability bar)

- ✅ 2026-09-24 — **FIXED STRUCTURALLY: 0 of 18 runs narrate.** Not by stronger instruction text — by changing `SO_askSnowflake`'s extraction to keep only the `type:"text"` elements AFTER the last `tool_result`. Measured over six captured runs: narration always precedes that boundary, the answer always follows it. `COALESCE(last_tool,-1)` lets a no-tool reply through; FLATTEN runs once via a window function so the agent is never called twice. Original note: **NARRATION PERSISTS ON 5 OF 21 RUNS DESPITE AN EXPLICIT PROHIBITION — strengthen the agent instructions.** *Scott + next session, Snowflake-side.* The deployed specification was verified character-for-character to contain "never write phrases like 'let me'" and "never refer to a chart", and runs still produced *"There's a verified query matching this exactly. Let me run it."*, *"I need to use the logical column name counterparty_name."* and *"I need to actually generate the chart before citing it."* Question phrasing modulates the rate (flat lookups 0/3, comparative questions 1/3 to 3/3) but does not remove it. **Also seen twice: the agent emits the same answer paragraph twice.** *Trigger: before the talk track quotes any answer verbatim.*
- ✅ 2026-09-24 — **FIXED by agent instructions v2**, verified over 3 runs: the asset-class answer now writes "FX forward", "Bond", "ETF". Original note: **THE AGENT WRITES RAW STORED ENUMS ONTO AN ANALYST SCREEN** — `fx_forward`, `etf`, `bond` in the asset-class answer. CLAUDE.md's display-vocabulary canon requires every enum reaching a screen to go through an `SO_*Display` rule; agent prose is a door the canon never anticipated, and the panel cannot map it without parsing answers. **Fix belongs in the agent instructions** ("write asset classes as Equity, Bond, ETF, FX forward"). *Trigger: the same session that strengthens the instructions.*
- ✅ 2026-09-24 — **FIXED two ways**: v2 forbids summing across currencies, and the chip's question now asks "…by currency?". All 3 runs report per currency and say the amounts cannot be combined. Original note: **SUP Q3 STATES A HOUSE TOTAL IN NO CURRENCY** — "25.62 billion" summing notionals across a EUR/GBP/JPY/CHF book. CLAUDE.md already ruled a house value-at-risk is **not computable from the data alone** (nothing carries an FX rate), which is why `SO_fxToUsd` exists and why every screen figure renders "USD eq." with its basis. The Ask panel bypasses that ruling. A settlements audience may challenge this number. *Trigger: before the supervisor Ask beat is scripted in Part 2.*

## PHASE 6 PART 1b — FOUND 2026-09-24 (relocation, click feedback, answer quality)

- ✅ 2026-09-24 — **INVESTIGATED AND CLEARED: chip 3 ran 5 consecutive times clean (25/23 s on the original specimen, 19/19/23 s on the hero).** No ceiling below 120s exists on this path — the Appian integration timeout covers the whole runtime, the 90s node limit is autoscaled-process-scoped, and Snowflake's agent timeout is 299s. **Not a timeout.** Most probable mechanism, stated as inference: a reply carrying no `type:"text"` element yields ARRAY_AGG → NULL → empty cell → the panel's failed branch; the shorter-answer instructions make that rare. A diagnostic build exists in git history if it recurs. Original note: **CASE CHIP 3 FAILED ON ITS ONLY RUN.** *Completion session.* "How does this trade's 11.8M EUR notional compare with typical Equity trades?" ran 46s on case `TRD026800` and returned the panel's failure state; chips 1 and 2 answered in 27s and 24s. Under the Part 1 bar a question that cannot clear stable substance is **replaced, not shipped**. Unknown whether the failure is intermittent (agent latency/empty text elements) or structural to the question. *Trigger: the completion session, before any answer is quoted in the talk track.*
- ✅ 2026-09-24 — **DONE: nine runs on the hero `TRD9DEMO01`** with a DEMO packet loaded through the console path and reset after. Chips composed correctly from hero data (Diamond Trust / Equity / 18.7M EUR). Original note: **Hero-with-packet verification of the case-detail panel still owed.** *Completion session.* Proved on fixture case `TRD026800` (Vanguard Prime / Equity / 11.8M EUR) because the environment was at clean baseline and loading a packet costs ~2 minutes plus triage. Composition and context resolution are proven; the hero specifically is not. *Trigger: the same session that re-runs chip 3.*
- [ ] **KNOWN LIMIT, recorded so it is not re-litigated: the question echo and an "Asking…" line cannot paint before the answer.** The call is synchronous and Appian paints once on return, so a local set in the same `saveInto` is invisible until the answer is already there. Delivered instead: the pressed button spins and disables (`loadingIndicator`), plus a standing "20–40 seconds" line that sets the expectation before the click. The only route to a true pre-call repaint is async process-and-poll, whose 30-second minimum refresh would add up to 30s to every answer — explicitly rejected. *Trigger: only if someone proposes the async route again; re-read this first.*

## PHASE 6 PART 1 — FOUND 2026-09-24 (Ask panels wired)

- [ ] **🔴 FINISH TASK 4: 3 of 6 seeded questions run, ONCE each — the three-run stability bar is unproven.** *Next session.* Remaining: supervisor "total notional at risk for High and Critical"; analyst "which risk factors most often drive fails for trades like this one" and "how does this trade's notional compare with typical trades of this asset class". Each call is 22–45s. A question that cannot clear the stable-substance bar is replaced, not shipped. *Trigger: before Part 2's talk track quotes any answer.*
- [ ] **Verify the analyst panel ON THE HERO with a packet loaded.** *Next session.* It was proved against fixture row `TRD030639` (Beacon Finance / ULVR LN) because the environment was at clean baseline. The context-line mechanism is proven; the hero specifically is not. *Trigger: the same session that finishes Task 4.*
- ✅ 2026-09-24 — **AUTHORED, awaiting Scott: `snowflake/agent-instructions.sql`.** One `ALTER AGENT … MODIFY LIVE VERSION SET SPECIFICATION` statement replacing only `instructions.response`; every other field restated character-for-character because Snowflake replaces the spec wholesale. Also confirmed the agent's ONLY tool is `cortex_analyst_text_to_sql`, which closes the Part 1 read-only gap. Original note: **the Cortex agent narrates its own process and promises a chart that does not exist.** *Scott + next session.* A1 returned "I'll look at the data model first…" and "The chart makes the gap visually stark…" — the panel renders no chart, so on stage that is a broken promise. These arrive as `type:"text"` elements and so CANNOT be filtered display-side without also filtering answer prose. **The fix belongs in SETTLEMENT_RISK_AGENT's instructions**, which is Snowflake-side and outside this session's scope. *Trigger: Part 2.*
- ✅ 2026-09-24 — **FIXED in `SO_askAnswerText` v5** — lines split, trimmed, empties dropped, rejoined with exactly one blank line, so spacing is normalised by construction rather than by chasing run lengths. Markdown `**`/`__`/backticks stripped too. Verified by `testRule`. Original note: **Answer blank-line cosmetics** — the `CHR(10)||CHR(10)` join compounds with the agent's own trailing newlines, giving 4–6 blank lines between paragraphs. Cosmetic; fix by joining on a single newline or trimming server-side. *Trigger: the same session that finishes Task 4.*
- [ ] **The 4000-character cap is an unmeasured constraint, not a solved problem.** The full `DATA_AGENT_RUN` transcript arrived truncated at exactly 4000 chars, which is why extraction moved into the SQL. Today's answers sit well under it, but a long answer could still be cut, and the source of the cap (Snowflake, the SQL API, or Appian) was never established. *Trigger: the first answer that looks cut off.*

## PHASE 5 — FOUND 2026-09-24 (mockup-authority / Matched-gate session)

- [ ] **`fixtures/p4-verify-redate.py` WRITES TO THE CURRENT DIRECTORY, NOT TO `fixtures/`** — *next session that runs the ritual; one-line fix.* Lines 110/117/122 are bare `open("p4-verify-cases.csv", "w")`. Run it from the repo root — the obvious place — and it drops three fresh CSVs at the root while the tracked copies in `fixtures/` stay stale, so a session can apply yesterday's dates believing it re-dated. Caught this session only because the `ls` afterwards showed a Sep 9 mtime on files the script had just claimed to write. **This is the harness-goes-stale failure with a path bug instead of a missing regenerate**, and the ritual is the one thing every rendering session runs first. Fix: write relative to the script's own directory (`os.path.dirname(os.path.abspath(__file__))`), so it cannot matter where it is invoked from.
- [ ] **`mockups/case_detail.html` is the next superseded-header candidate, and it is now the stalest file in `mockups/`** — *Scott: one word and the next session applies it.* The 2026-09-24 mockup-authority ruling says a mockup whose screen is built, gated and verified gets a superseded header rather than regeneration. `SO_caseDetail` is at **v9** and diverged further this session (Matched stamp, "Other desk", relative column widths); the mockup is dated 2 Sep and already carries three recorded errors — "last 30 days" (TODO Done 2026-09-09), the overnight-scoring line (line 277), and the USD/EUR currency label. The brief named only `supervisor_command.html`, so nothing was applied here. *Trigger: Scott's go-ahead, or the next session that touches `SO_caseDetail`.*
- [ ] **The `SO Demo Admins` group DESCRIPTION still reads as the Demo-Admins-only model** — *Designer, Scott; cosmetic but it is the first thing a new SC reads in the Admin Console.* It says the group "Gates the SO Demo Admin site … and nothing else" and "Membership is the presenting SC's own account", with no mention that a presenter also needs `SO Supervisors`. The 2026-09-24 ruling is now in CLAUDE.md, GETTING_STARTED and BUILD_PLAN; the group object is the last place carrying the old reading. Not changed from the session — group administration is not available over the Dev MCP (supplemental §3) and the brief forbade identity changes. *Trigger: next Designer visit.*

## PHASE 5 PART C — FOUND IN THE DRESS REHEARSAL (2026-09-24)

- [ ] **🔴 THE DEMO SCRIPT DOES NOT EXIST — the single biggest gap to a cold run.** *Scott / talk-track work.* No document tells a presenter what to say, in what order, which screen to open when, or **what Alex clicks**: `hero-trade-spec` specifies the agent's behaviour ("propose a borrow but hold it for a human") and never names the analyst's action, while four affordances are equally present (Record Disposition, Assign, Escalate, Add Comment). **DECIDED 2026-09-24: the hero's scripted analyst action is `Record Disposition → Settled - Borrow Executed`** — the disposition `SO_remediationForReason` maps to `insufficient_securities`, and the one that makes the agent's proposed cover borrow the thing the analyst accepts. Exercised end to end in the rehearsal. What remains is the script around it, not the choice. **Demo-script inputs collected so far:** (i) that action; (ii) do not press Load twice mid-demo — it re-stamps the scoring batch away from the case timestamps; (iii) if the console shows the amber "Loaded before today" line, Reset then Load before presenting, or every cutoff on the book reads as past. *Trigger: now — it blocks cold-running by another SC.*
- [ ] **RULING WANTED: "ZZ-CLICK" is not defined anywhere in the repo.** The Part C brief asked for a "ZZ-CLICK delete-click residual check per its spec"; grepping every `.md` returns zero matches. The residual that exists is item 82 below, whose spec hands the click to a human and asks a session to *stage* a two-click verification. **Either point at the intended spec, or authorise staging the two-click verification as its own task** — it is a data write, which the rehearsal forbade. *Trigger: now.*
- [ ] **DEFECT CAPTURED, cause unconfirmed: the counterparty recent-fails card renders Instrument `—` and Value `0` on every row** for `alex.analyst` on the hero's case detail, while its own summary line correctly reads "4 of 5 on insufficient securities". Likely the desk-security boundary already anticipated in the Phase 4 carried items (SO Trade is desk-secured; a counterparty's fails span desks) — which would make it row security working while the screen says "0" rather than "not visible to you", the absent-vs-invisible trap CLAUDE.md §4 names. **One read of the same card as `sam.supervisor` settles it.** Not diagnosed mid-rehearsal by the brief's own rule. *Trigger: the next session that may touch case detail.*
- ✅ 2026-09-24 — **DONE. CLAUDE.md rewritten to match reality:** the status→action mapping is verified as a persona through sail, per case status, like any other content-and-behaviour check; only the header's *geometry* stays a browser check. Original note: **record-header related actions ARE reachable through sail.** The file says the status→action mapping is "a browser check and only a browser check" because the header renders outside any interface a session can reach. The rehearsal read `Record Disposition · Assign · Escalate · Add Comment` off the header as `alex.analyst` and drove one to completion. *Trigger: the next session permitted to edit CLAUDE.md.*
- [ ] **TALK-TRACK LINE (captured into the demo-script inputs above): do not press Load twice mid-demo.** A second press duplicates nothing, but `SIMULATE_FEED` is idempotent and re-stamps `SCORED_AT`, so the batch band jumps (observed `10:42` → `10:47`) while the cases keep their original creation times — a gap between "scored" and "case created" that never happened. *Trigger: demo-script writing.*
- [ ] **The sub-minute double-Load race is still open.** The rehearsal's second press was ~4 minutes later with all cases created and two resolved, so the logged "two Loads within ~1 minute both find the same `New` case" was **not** reproduced and is not closed. *Trigger: if it is ever observed; otherwise close by inspection of the intake guard.*
- ✅ 2026-09-24 — **BUILT: both Ask panels are live against Snowflake Cortex.** The supervisor shell is gone and the analyst screen gained one (new design, placed by ruling at the foot of the queues). Original note: **Phase 6 Ask panel confirmed visibly unfinished on the supervisor house view**: `ASK ACROSS THE BOOK` renders with a DISABLED text field and "Pick a question above, or ask your own." Either build it (Phase 6) or give the presenter a line. Recorded here so it is not discovered on stage.

## PHASE 5 — FOUND 2026-09-24 (null-guard sweep round)

- ✅ 2026-09-24 — **RULED AND SWAPPED to `resolvedOn`**, both grouping reads together, guards kept; measured with a probe because the card renders identically under either key. Original note: **RULE: the straight-through trend groups by `modifiedOn`, but `resolvedOn` is the right field.** *Scott.* The card's own comment says "grouped by the DAY they resolved"; `resolvedOn` is that field and **is populated on process-written rows** — precisely the rows now excluded from the axis. Swapping it places them instead of dropping them. Left alone because it changes what the axis means rather than adding a guard, which was this session's scope. *Trigger: now — a one-line change once ruled.*
- [ ] **Edge, unreachable while fixtures exist:** if *every* resolved case were dateless, the trend card would read "0 cycle on record" (singular, and odd) with `resolvedCount > 0`. The P4-VERIFY fixtures always contribute a dated cycle and the console refuses to delete them, so this cannot occur today. *Trigger: any change making the fixture set deletable, or a `resolvedOn` ruling that empties the grouping differently.*
- [ ] **Any future null-guard sweep of this class must search BOTH forms** — the UUID-qualified field read *and* the bare map-key read (`index(fv!item, "modifiedOn", null)`). The line-351 defect was a map-key read; a UUID-only search would have missed it. *Trigger: the next sweep for a nullable field.*

## PHASE 5 — FOUND 2026-09-24 (persona verification round)

- [ ] **The agent's confidence on story 03 varies run to run — 0.90 on 2026-09-23, 0.88 on 2026-09-24.** Both comfortably above the 0.80 gate, both straight-through, so the lane is stable; only the printed number moves. Recorded so a future session does not read it as drift or tune against it. *Trigger: any session that sees story 03 land outside the straight-through lane — that WOULD be a defect.*
- ✅ 2026-09-24 — **The view-all toggle, the rail binding and the non-selectable queues are behaviourally verified**, not merely configured: the footer link was clicked (4 → 8 rows and back), two rows were selected and produced full rails, and sail reports the escalated and cleared grids `[READONLY]` with no row handles.

## PHASE 5 — FOUND 2026-09-23 (watchlist restructure session)

- ✅ 2026-09-24 — **FIXED, and the defect class swept.** Line 351 guarded symmetrically with 345; the empty cycle bucket dropped as well, because the guard alone rendered a line chart with a `null` category plotting a fabricated 100% → 33% fall (measured on a throwaway copy). Dateless resolved cases are counted in every figure and excluded only from the trend axis. Verified: `sam.supervisor` opens the Supervisor page with a demo loaded. Original note: **🔴 BLOCKING PART C — `SO_supervisorCommand` breaks whenever a demo is loaded.** *Scott rules, then one edit.* Measured with a control at BOTH scopes: `testInterface` errors with the demo loaded (`at function 'text' [line 351]: A null parameter has been passed as parameter 1`) and returns `error: null` after Reset; **and as `sam.supervisor` via sail the page returns an HTTP 500 error page** (`APNX-1-4198-000`, "Error Evaluating UI Expression") with the demo loaded, loading the full house view cleanly after Reset (2026-09-24). A supervisor clicking their own tab mid-demo gets an error page, not a degraded screen. **Cause:** `local!cycleList` (line 345) guards `modifiedOn` for null; `local!cIdx` (line 351) calls `text()` on the same field unguarded — and a process-written case has a NULL `modifiedOn` (measured: all three demo cases NULL, all three fixture resolved cases populated). Every demo run creates exactly one straight-through resolution, so the supervisor screen is dead for the whole demo window. **Fix is one line**, copying line 345's guard onto 351. Not applied: the 2026-09-23 brief put supervisor screens out of scope. *Trigger: now — it blocks the dress rehearsal.*
- ✅ 2026-09-24 — **RULED 6 and built.** `local!nearest` is 6; the hero sits 4th of 9 by cutoff with two rows below it, verified as `alex.analyst`. Footer copy derives from the local, so it followed without a second edit. Original note: the hero landed 4th of 9, the last row visible before the view-all footer.
- [ ] **RULE: the watchlist filter row was removed** (priority / fail reason / search) because the v5 mockup has none and the partitioned, cutoff-sorted queues earn it less. *Scott — reversible in one edit if you want it back.*
- ✅ 2026-09-24 — **RULED: 60 words accepted, no change.** Recorded in the object header so the next edit knows there is no headroom. Original note: **the console is now exactly at its 60-word budget** (CLAUDE.md says "under 60"). The explainer costs 39 words where the two captions cost 17. The copy is yours and verbatim, so the budget gave; either grant the explainer an exemption in CLAUDE.md or accept there is no headroom. *Scott.*
- ✅ 2026-09-24 — **Persona sail sessions added by Scott and used.** `alex.analyst` and `sam.supervisor` both live. The full persona verification ran from the terminal: page gate, both watchlist data states, the view-all click, row selection and the rail, and the supervisor regression. Original note: **Persona sail sessions did not exist on this machine** — `~/.sail-*` holds only another build's `sd.accountant` / `sd.assetmanager`. Every persona-scoped check is therefore a browser check until `alex.analyst` and `sam.supervisor` are logged in once (`SAIL_USERNAME` / `SAIL_PASSWORD` + `sail login --data-dir ~/.sail-<persona>`). *Scott, once; it is permanent after that.* *Trigger: the next session asked to verify as a persona.*
- [ ] **`reference/toolchain.md` version pins are one release behind** — Dev MCP and sail both report 26.6.95 (`20260911-210447`) against the pinned 26.6.90. App Market reports the site up to date, so the pin is what is stale. *Trigger: say "run the update procedure".*

## PHASE 5 PART B2 — INTAKE + CONSOLE (2026-09-11)

- [ ] **Live pass from the console — PARTIALLY RUN 2026-09-22, blocked on the intake defect below.** *Scott, blocking Part C.* Steps 1–5 ran: Load returned `OK run=B2TEST trades=15 predictions=15 high_or_critical=3`, but only 1 case appeared (story 01, the hero, correctly at 0.62 / Awaiting review). Steps 6–9 (REFUSED path, reset, both-sides-clean) were not run. Re-run whole once the loop is fixed. The script is the 2026-09-11 close-out in git history (`git show 2a4ec42:Closeout.md`).
- ✅ 2026-09-22 — **FIXED: intake created only the first case of the plan.** A flow returning to an already-fired XOR gateway never re-activates it, so the loop did one pass and sat `ACTIVE` in silence. `SO_intakeRun` now loops back to the take-item script with the continue/stop gateway downstream of the increment. Verified live: all three B2-TEST cases exist, `toCreate` is empty on re-run. Promoted to appian-supplemental §9 (and corrected that file's own loop recipe).
- ✅ 2026-09-22 — **ANSWERED: the id 53/54 gap is the 2026-09-09 cascade measurement** — two throwaway cases plus two comments and two events, created and deleted to prove the cascade (CLAUDE.md records it). Not a defect.
- ✅ 2026-09-22 — **RULED AND BUILT: story 02 escalates by policy.** A breached window (broker lag > hours remaining) now escalates in `SO_triageCase` ahead of both other lanes; the agent's score is recorded but does not decide it. packet-spec §5 and CLAUDE.md updated.
- [ ] **DECIDE before the asset goes to another SC: the fixed run name removes multi-SC concurrency.** *Scott.* `DEMO` is now the only run name, so two SCs cannot demo on this environment at the same time — the per-run naming that allowed it was traded for a console with nothing to type. Fine for one presenter; needs a second environment, or the name field back, for more. *Trigger: the first time a second SC wants the asset.*
- [ ] **No integration test screen exists** — the console's REFUSED path is now unreachable, so a malformed-name refusal is exercised by calling `SO_simulateFeed` from its integration object in Designer. Build a small test screen if name-guard testing becomes routine. *Trigger: Part E / SC setup doc.*
- ✅ 2026-09-22 — **Deleted `SO_zz_probeQuery`** on Scott's word (`deleteInterface` → "Deleted successfully").
- ✅ 2026-09-24 — **CLOSED on Scott's word: both terminated in the Admin Console on 2026-09-22.** Not independently verifiable from a session — process instances are not readable over the Dev MCP at all (no listing, history or status tool), so **the Admin Console is the authority for instance state** and a session can only record the ruling. Original note: **Clear two parked intake instances** — the console's 2026-09-22 live run and this session's first attempt are still `ACTIVE` at the old gateway. They cannot be cancelled over the Dev MCP. Harmless; clear them from the Admin Console when convenient. *Trigger: next Designer/Admin Console visit.*
- [ ] **Console repeat-press behaviour** — on a second Load of the same run the console appeared green while Snowflake's single `SCORED_AT` proved the procedure ran once. Still unexplained; the redesign did not touch result handling. *Trigger: Part C, if it recurs — capture the panel text verbatim.*
- [ ] **TOOLING LIMIT: process instances are not readable over the Dev MCP** — no listing, history or status tool, and `testProcessModel` starts a new instance rather than reading one; the only instance-status tool belongs to the banned `appian-runtime` server. Consequence: "did the loop stall or exit?" cannot be answered from a session, so intake verification must be designed to prove itself through rows and audit events instead. *Trigger: the fix session; re-check the tool list then.*
- [ ] **RULE: process-model security for SO Demo Admins** — *Scott, before any SC other than Scott presents.* `SO_simulateRun`, `SO_resetRun` and `SO_intakeRun` carry the app default (viewer SO Users / Supervisors / Analysts, admin SO Administrators). A presenter who is only in SO Demo Admins cannot start them — the console then shows "could not start" — and cannot read the result. Identity was closed for B2, so nothing was granted.
- ✅ 2026-09-24 — **CLOSED as ruled and implemented: presenter must be in SO Supervisors — now BOTH document and rule.** The rule is the `SO_demoAdminConsole` preflight, which names the missing group instead of crashing; the documents are CLAUDE.md:266/405, GETTING_STARTED and BUILD_PLAN. The row-security consequence in the original note was understated: without `SO Supervisors` the presenter is not in `SO Users` either, so it holds viewer on **no** record type and the console does not render at all — the desk-scoped-intake failure is what an `SO Analysts` presenter would get instead.
- [ ] **RULE (optional): the integrations' SO Demo Admins viewer is folder-inherited** from Rules folder `02e190ca-6878-4c1c-a355-11d377181211` (direct viewers SO Users + SO Demo Admins), so it also covers every other inheriting object in that folder. The effect is as intended; the mechanism is reported and left unchanged.
- [ ] **Reset while triage is still running** — `SO_triageCase` writes by caseID ~45 s after it starts; deleting a case mid-triage is unmeasured, and cancelling orphaned processes is still open. *Trigger: Part C — observe, then decide on a guard or a cancel step.*
- [ ] **Two Loads of the same run within ~1 minute can triage one case twice** (both intakes find the same `New` case). *Trigger: Part C, if observed.*
- [ ] **`SO_triageCase` description is stale** ("STUB parameters", "AGENT_NODE_PLACEHOLDER") — the model has had the agent node since Phase 3. Description-only fix. *Trigger: next session that edits `SO_triageCase`.*

## PHASE 5 — LATER ITEMS DISCOVERED IN PART A (each with a trigger)

- [ ] **REVIEW: packet run date is the UTC date (`SYSDATE()::DATE`), not the hero's `CURRENT_DATE`.** This is a judgment call from P9's clock row: the Snowsight session is Pacific, so `CURRENT_DATE` is a day behind APAC mornings. Consequence: a New York viewer after 20:00 EDT sees packet trades settling "tomorrow". Reverting is one line in `SIMULATE_FEED`. *Trigger: Scott's review of the Snowsight run.*
- [ ] **`TRADE_PREDICTIONS.SCORED_AT` defaults to `CURRENT_TIMESTAMP()`** — any writer that omits it stamps the Pacific session clock, which Appian reads as UTC (seven hours old). *Trigger: the Phase 8 `run_inference.sql` rewrite must write `SCORED_AT` explicitly as `SYSDATE()`; also check whether the current baseline stamps were written that way.*
- ✅ 2026-09-11 — **Console run-name guidance built (Part B2):** placeholder `e.g. 1012-ACME` and the rule as field instructions. Deliberately NOT blocking — the procedures' REFUSED message is the enforcement (B1 ruling) and must be reachable for the REFUSED test. Only `P4-VERIFY` is blocked Appian-side.
- ✅ 2026-09-11 — **"One more step, by hand" box REMOVED** — Delete now runs `SO_resetRun` (Appian cases first, then `RESET_SESSION`).
- [ ] **Intake reading `TOP_RISK_FACTORS`** — later phase. It needs a Designer re-sync of SO Trade Predictions, and VARIANT mapping on a record type is **untested**. *Trigger: Phase 8 model work, or any beat that shows risk drivers.*
- ✅ 2026-09-10 — **RULED: packet predictions are baseline-shaped.** All 15 rows author `TOP_RISK_FACTORS` from the measured vocabulary under the coherence rule — factors never contradict the story's fail reason or lane (packet-spec §4). Two shape details are confirmed by P9 branches 5–6.
- [ ] **Talk track roadmap line: a probability-floor constant (NOT built).** *Trigger: demo-script writing.*
- [ ] **Matched stamps render in the viewer's timezone from a UTC read** — trade-date 18:12 NTZ shows 14:12 for a New York viewer, exactly as the baseline does. *Trigger: demo-script writing (one talk-track line, no build change).*
- [ ] **`SO_regionDisplay`'s description still says NA/EU/UK/APAC.** The expression maps all four stored values correctly; only the description is stale. *Trigger: next time the rule is opened.*

## PHASE 4 CARRIED ITEMS (swept 2026-09-09 at build-scope close)

Five items leave Phase 4. Each has an owner and a firing condition; none is parked on "later".

- ✅ 2026-09-10 — **Add the first member to `SO Demo Admins`** — *done by Scott in Designer.* Not re-readable from a session: group membership reads return 403 over the Dev MCP. Original note: Group membership cannot be set over the Dev MCP (403 on `addGroupMembers` / `getGroup` / `listGroupMembers`, including against long-existing groups this account lists fine). **The group is empty, so `settlement-ops-admin` is invisible to everyone.**
- ✅ 2026-09-10 — **Reorder the record actions so Record Disposition leads** — *done by Scott in Designer; VERIFIED by `listRecordTypeActions` readback: Record Disposition · Assign · Escalate · Add Comment.* Original note: Measured as not settable over MCP: `relatedActionShortcuts` accepts an order and returns the record type's own.
- [ ] **Re-home the Snowflake logo PNG into SO Artifacts and repoint `SO_SNOWFLAKE_LOGO`** — *Designer, Scott.* The cross-app viewer grant works today, but a shipped multi-SC asset must not depend on another application's object; a security reset over there re-breaks every SO screen at once. `SO_MARK_USE_LOGO: false` is the one-flip recovery meanwhile.
- ✅ 2026-09-24 — **RESOLVED — the BUILD moved, not the ruling.** `SO_caseDetail` v9 opens the gate on the ruled condition, which was never "the field is populated" but "a stamp exists that was authored on this clock". Implemented as a 7-day recency window, because **presence discriminates nothing**: measured this session, every row on the instance reads `isMatched = Y`, fixture stamps span 2023-01-10 to 2025-05-02 and packet stamps read 2026-09-23 — ~16 months against ~1 day. Verified as `alex.analyst` on both paths: hero `Matched Yes · 23 Sep 14:12`, fixture `Matched Yes` with no stamp. **The stamp carries its day whenever it is not today's** — a packet trade matches on trade date and settles the next, so a bare `14:12` beside today's cutoff would have re-created the very contradiction the 2026-09-09 ruling removed. Original note: **the Matched-time gate divergence.** `Matched Yes` still renders without a time on a packet row. CLAUDE.md records the stamp gate as opening in Phase 5 once packet trades author `MATCHED_AT` on the current clock; the packet does author it, and the screen still shows state only. Ruled intent and built screen have diverged — decide which moves. *Trigger: the next mockup pass.*
- ✅ 2026-09-24 — **CLOSED BY RULING, both halves.** (1) *Layout:* the mockup-authority ruling (CLAUDE.md, Working style) transfers authority to the built `SO_supervisorCommand` at its gate; a stale mockup gets a **superseded header, not regeneration**, and that header is now on the file naming the wide-left/rail-right divergence explicitly so no session restores the old shape from it. (2) *Overnight copy:* **already true in the build and nothing to apply.** The built watchlist carries on-arrival wording at `.work/SO_analystWatchlist.sail:753` — *"Scored in Snowflake on arrival, not overnight — the packet is scored as it lands."* The mockup header string `"scored overnight batch 05:00"` no longer exists (removed in the v5 rewrite). The one surviving instance is `mockups/case_detail.html:277`, a mockup now governed by the same ruling. The spec never prescribed exact replacement wording, so none was invented. Original note: **Next mockup pass: `mockups/supervisor_command.html` to the built layout, AND the "overnight scoring" copy** — *next mockup pass.* **Overnight copy (added 2026-09-10):** the watchlist mockup header "scored overnight batch 05:00" and the case-detail mockup's "Overnight scoring flagged…" become on-arrival wording, because packet trades score on arrival. **The built watchlist's empty-state message stays as is (ruled)** — it is still true for the overnight-scored baseline. Layout note: The build was reorganised by ruling into wide-left / rail-right; the mockup still has the trend chart beside the runway and reasons/queues inside the left column. It is the structural authority, so leaving it behind means the next session "restores" the old shape from it.
- ✅ 2026-09-24 — **CLOSED BY RULING.** The click is exercised: three live human-clicked Resets with the was/now evidence line read back in the 2026-09-22 and 2026-09-24 sessions, plus `snowflake/verification-summary.sql` checks 20/21, which prove tag-scoped deletion is prefix-safe at any code length (runs `VFY1` vs `VFY12`, packet-spec §1). The same citation closes the console's was/now human check. `a!deleteRecords` in a `saveInto` remains session-uninvocable, which is a tooling boundary, not an unverified link. Original note: **Close the Reset delete-click residual** — *next session, then Scott.* `a!deleteRecords` lives in a `saveInto` and cannot be invoked from a session, so the button click is the one link in the reset path never exercised end to end. Everything up to it is render-verified and the cascade is measured. **Next session seeds throwaway rows under a known run name and hands Scott a two-click verification** rather than leaving it open indefinitely.


Open items by class. Sessions add discovered items unprompted; any response that touched this file ends with a `TODO changes:` line.

## Browser checks (Scott's — not doable from the Dev MCP session)

- ✅ 2026-09-10 — **BLOCKING FOR GATE D — add your account to `SO Demo Admins` in Designer.** Done by Scott. Group membership cannot be set over the Dev MCP: `createGroup` succeeded, but `addGroupMembers` / `getGroup` / `listGroupMembers` all return HTTP 403 "Group not found or access denied", including against long-existing groups this account can list. **The group is empty, so `settlement-ops-admin` is currently invisible to everyone.** One Designer edit unblocks it.
- [ ] **GATE D browser pass — Demo Admin.** Open `settlement-ops-admin` → Admin. (a) Nine Verify Ready lines, colours as listed in the build log — 50000/50000 green, three neutral grey dots, 9-of-9 green, agent line green; (b) type `P4-VERIFY` into Reset and confirm **REFUSED** with the button disabled; (c) ✅ **CLOSED BY RULING 2026-09-24** — the click is exercised by three live human-clicked Resets with the was/now line read back (2026-09-22, 2026-09-24) plus verification-summary checks 20/21; it is no longer the last unverified link. Original: type a live throwaway tag and confirm the before-counts and the enabled button, then click it; (d) confirm Simulate Feed is inert and reads as deliberately unfinished rather than broken.
- [ ] **Confirm the Demo Admin site never appears on the analyst site**, and that `alex.analyst` / `sam.supervisor` cannot reach `settlement-ops-admin` at all.

- ✅ 2026-09-24 — **CLOSED BY RULING — this is the trap the mockup-authority rule was written to kill.** The item's own fear ("the next session will restore the old shape from it, since the mockup is the structural authority") is exactly the premise now retired: authority transferred to the deployed screen at gate C. The file carries a superseded header naming the divergence. Original note: **Mockup `supervisor_command.html` is OUT OF DATE with the build** — the layout was reorganised by ruling (wide left: runway + desk grid; rail right: ask, trend, reasons, queues) where the mockup's DOM has the trend chart beside the runway and reasons/queues inside the left column. **Fix the mockup in the pre-Phase-5 pass** or the next session will restore the old shape from it, since the mockup is the structural authority.
- [ ] **GATE C browser pass — Supervisor Command. (a) and (b) are CLOSED 2026-09-24 via sail**, leaving only the geometry items. ✅ (a) the source line reads exactly "house view across 5 desks · 17 cases in scope"; ✅ (b) `alex.analyst`'s page list is Watchlist + Cases only — the Supervisor page is absent, not merely hidden. Remaining: (c) **the fold** — is the Escalations card reachable at 1440px without hunting? It is the last thing on the page and the one a supervisor most needs; if the screenshot shows it buried, that is a defect, not a preference; (d) **the residual column gap** — the chart is now `height: "MEDIUM"` and the rail cards carry `marginBelow: "STANDARD"`; exact bottom-alignment is **not natively achievable** (a fixed `a!cardLayout(height:)` scrolls its overflow, which is clipping, and there is no stretch-to-fill or cross-column align), so measure the residual and say whether it reads as untidy or as normal card rhythm — **do not ask for it to be closed by fixing card heights**; (e) do the rich-text bars line up as bars rather than wrapping or truncating at MEDIUM_PLUS — **the reason bars were rebuilt** from a starved side-by-side slot to the proven queues pattern at a 14-glyph budget, so this is the check that the fix took; (e2) do all six KPI sub-lines sit on ONE line, leaving the band level — the character budget (~37) is an estimate, this is where it is confirmed; (f) **chart type** — the render tree cannot distinguish a column chart from a bar chart (identical serialisation), so "hours along the bottom, not down the side" is a browser check even though the source says `a!columnChartField`.
- [ ] **Snowflake logo as a PERSONA.** The rights grant is confirmed by security readback and both screens render the logo document with zero fallbacks — but as `scott.thorn`. The failure this guards against only ever appeared on a persona login. Open the watchlist as `alex.analyst` and as `sam.supervisor`: expect the logo, no broken image, and **no page-level error**. If it errors, flip `SO_MARK_USE_LOGO` to false and every screen recovers.

- ✅ 2026-09-09 — **Gate A screenshots: Scott is supplying them himself.** The in-session blocker (no Chrome extension connected; in-app browser lands on Google SSO) is moot. Everything geometric — wrapping and truncation at 1440px, whether the one-scale rail reads calm, whether the selection tint reads as selected — is settled on his click-through.

- [ ] **THE STATUS→ACTION MAPPING IS NOW A BROWSER CHECK AND NOTHING ELSE — re-scoped again 2026-09-09 when the actions moved to the record header.** The in-card bar was the only observable copy of the rule; with it gone, the record type's visibility expressions are the sole gate and they render outside the view, so `testInterface` sees no action set at all. Click one case per status as `alex.analyst` and confirm: **New / Awaiting review** → Assign · Add Comment · Record Disposition · Escalate; **In remediation** → Record Disposition · Escalate · Add Comment; **Escalated (SO-39, SO-40)** → Record Disposition · Add Comment; **Resolved (SO-41)** → Add Comment only. Also confirm each action opens and writes, and that the Related Actions tab is gone.
- [ ] **Record header TITLE renders outside every interface a session can reach** — same class as the action shortcuts. `titleExpression` is set and its two-hop relationship path normalised into a record-field URN (so the reference resolves), and the same path returns the ticker in the view's own query (so the data is there), but **the rendered string is unverified**. Expect `TRD001089 — CHFJPY 1M FWD  ·  Case SO-39`. Check the em-dash and the two-space `·` separators survive, and say whether the case-id tail earns its place — dropping it is a one-line change.
- ✅ 2026-09-10 — **Header action ORDER is the record type's, not the view's** — reordered by Scott in Designer; the readback reads Record Disposition · Assign · Escalate · Add Comment. The rendered header order remains a browser glance. — measured: `relatedActionShortcuts` was sent Disposition/Assign/Escalate/Comment and read back Assign/Comment/Disposition/Escalate. If Record Disposition should lead, reorder the actions on the record type in Designer; nothing on the MCP surface does it.
- [ ] **Record links are structurally verified, not clicked.** Ticker → SO Instrument, counterparty → SO Counterparty, five trade refs → SO Trade, each confirmed by `recordRef` in the tree. **Expect some trade links to open as "no access" for `alex.analyst`** — SO Trade is desk-secured and a counterparty's recent fails span desks. That is row security working; confirm it degrades gracefully rather than erroring the page.
- [ ] **alex/sam desk boundary** on both cases and trades: `alex.analyst` sees only EQ_FLOW, `sam.supervisor` sees all. The Dev MCP account is supervisor-scoped, so the analyst-side restriction cannot be observed from a session at all.

- ✅ 2026-09-09 — **FX approach APPROVED as built.** Indicative static rates in one rule, every derived figure labelled "USD eq." with the conversion basis in the KPI tooltip, stable signature. Recorded in the CLAUDE.md display-vocabulary canon. **Phase 5 still swaps the rule body for a real rate lookup** — a small Snowflake rate table joined at query time, or a daily rate set by the packet builder; no screen moves when it does.
- [ ] ~~**`SO_fxToUsd` holds INDICATIVE STATIC RATES and Scott can reverse this.**~~ Superseded by the approval above; kept for the reasoning: There is no FX source in the data, so a cross-currency house total ("value at risk across all desks") cannot be computed from the data alone. Built as a labelled approximation: one editable rule, every derived figure rendered as "USD eq." with the basis in a tooltip. **Phase 5 replaces the body with a real rate lookup** — a small Snowflake rate table joined at query time, or a daily rate set by the packet builder. The signature is stable so no screen moves.
- [ ] **`SO_riskTierDisplay` swapped into the WATCHLIST 2026-09-23; CASE DETAIL still pending.** The watchlist's inline `upper(left())&lower(mid())` on counterparty `riskTier` is gone, replaced by `rule!SO_riskTierDisplay`. Case detail still title-cases inline. *Trigger: the next pass that opens case detail.* Original note: Both screens still title-case counterparty `riskTier` inline. They are gate-A and gate-B ratified and the inline form renders correctly on all four stored values, so the swap waits for the next pass that opens either screen — the trigger this item already carried.
- [ ] **Phase 5 packet: seed multi-cycle resolved cases so the straight-through trend is a trend.** Today there is one resolved cycle, so the chart correctly renders "1 cycle on record — a trend needs at least two". Same packet work unlocks the reason-mix day-over-day deltas and the "vs yesterday" KPI sub-line, all three of which are currently dropped for the same reason: a single-session fixture has no prior cycle.
- [ ] **Desk mix is heavily skewed: 13 of 17 P4-VERIFY cases are EQ_FLOW**, leaving CREDIT / FI_TRADING / FX_DESK / ETF_MM with one case each. The house view is therefore correct but lopsided — one desk carries 10 of 14 open items. **Not seeded without asking**, per the gate C brief. If the supervisor screen should look like a real book, the packet is where that is fixed.

## Pre-Phase 4

- ✅ 2026-09-09 — **Recent-fails card built with NO time-window claim.** "Recent settlement fails", five most recent per counterparty via SH→Counterparty with SH→Trade→Instrument traversal, real dates shown. The mockup's "last 30 days" would render empty for every counterparty. The mockup file still says "last 30 days"; relabel it in the pre-Phase-5 mockup pass.
- [ ] **Onward-deliveries "may cascade" branch is UNEXERCISED by fixtures.** `SO_onwardDeliveries` returns 0 on every P4-VERIFY case — it counts our SELL trades in the same instrument settling within two days after the case trade's `expectedSettlementDate`, and the fixtures point at baseline trades whose settlement dates are historic and sparse. Zero is the mockup's own state ("None next 2 cycles — contained"), so the card is correct as it renders; but the non-zero branch has never been seen. **Fix belongs in the Phase 5 packet:** author the hero packet so at least one case has a real onward sell in window, and the cascade story becomes demonstrable rather than theoretical.
- [ ] **Recent-fails empty state is unexercised.** Every baseline counterparty has 147–276 failed settlements, so "No settled fails on record for this counterparty" cannot be reached from current data. Branch is written. Same Phase 5 packet fix if it matters.
- [ ] **`SO_riskTierDisplay` — the last casing-instead-of-mapping site.** Counterparty `riskTier` is title-cased inline (`upper(left(x,1)) & lower(mid(x,2,n))`) on both `SO_analystWatchlist` and `SO_caseDetail`. It renders CORRECTLY on all four stored values — measured 2026-09-09 in the enum-leak audit — so this is fragility, not a live defect, and the watchlist is gate-A-ratified. Build the rule and swap both sites in the next pass that opens either screen. Same family as `SO_assetClassDisplay`, which was the same expression and DID leak (`Fx_forward`, and it would have produced `Etf`).
- [ ] **The escalate process node still composes audit text from the RAW fail reason.** Fixture events 118/120 stored `Escalated by agent: funding_gap — …` and were rewritten by hand to the display label; the live path (`SO_triageCase` node 23 / `SO_escalateCase`) will re-emit the raw enum on the next real run. Process-composed audit detail is analyst-facing and obeys the display-vocabulary canon. Route it through `SO_failReasonDisplay`, lower-cased into the sentence, when the process models are next opened.
- [ ] **Throwaway `SO_zz_probeQuery` (`_a-0001f054-7a62-8000-9c49-011c48011c48_562148`) is still in the application.** Self-labelled "THROWAWAY … Delete after use" from an earlier session. Left in place deliberately this pass — deleting a design object is irreversible over MCP and it belongs to a session that is not this one. Delete on Scott's word, or at the gate C cleanup.
- ✅ 2026-09-09 — **Matched timestamp RULED: state-only, and the stamp is conditional on the Phase 5 packet.** "Matched Yes / No" with no time on fixture or baseline data, because `SO Trade.matchedAt` is baseline, is never mutated by a demo run, and is therefore stale by construction — no honest source exists. The information is not lost, it is deferred: `hero-trade-spec.md` §3a now REQUIRES packet trades to author `MATCHED_AT` relative to now (`cutoff - 4h`), and authoring rule 4 generalises that to every timestamp on a packet trade. The display condition is "a stamp exists that was authored on this clock", not "the field is populated". Recorded in the CLAUDE.md display-vocabulary canon.

- [ ] **Recent-fails card: "Recent settlement fails", no time-window claim** (decided 2026-09-02). `SETTLEMENT_HISTORY.ACTUAL_SETTLEMENT_DATE` spans **2023-01-08 → 2025-07-20** against a system date of 2026-09-02 — the newest fail is ~13 months old, so `case_detail.html`'s "last 30 days" window renders **empty for every counterparty**. Re-dating is ruled out by the data-safety policy. Phase 4 build: query the **N most recent fails per counterparty** (no date filter), showing real dates. Mockup: relabel the card to **"Recent settlement fails"** in the pre-Phase-4 mockup pass.

- [ ] **Phase 5 — `Verify Ready` baseline counts must exclude the reserved range.** A loaded packet otherwise reads as corruption. Count with `WHERE TRADE_ID NOT LIKE 'TRD9%'` (expect 50,000 trades / 50,000 predictions). Note `SETTLEMENT_HISTORY` stays 50,000 while a hero is loaded — the hero is an open trade with no settlement outcome, so trades 50,001 vs history 50,000 is **correct**, not a defect.
- [ ] **Phase 5 — packet builder consumes `hero-trade-spec.md`.** That file is the contract for the hero trade and for both mockups; if a number changes there it changes in the mockups too. Builds `SIMULATE_FEED(session_id)` / `RESET_SESSION(session_id)` as Snowflake stored procedures called over the SQL REST API with the existing PAT. `snowflake/hero-trade-insert.sql` and `hero-trade-delete.sql` are the manual equivalents / procedure bodies.
- ✅ 2026-09-02 — **`cutoffTs` derivation resolved: `SO_createTriageCase` accepts `cutoffTs` as an explicit input parameter**, so the packet builder sets it directly and needs no override. Verified by tool schema and by a live case creation that stored the passed value exactly. Spec §3b stands as written.
- [ ] **Watchlist mockup: TRD044567 row identity → `EURUSD 3M FWD`.** `analyst_watchlist.html` currently shows `S TTE FP TotalEnergies` on that row. The French large-cap identity was dropped 2026-09-02 when `INS0142` was pinned to `EURUSD 3M FWD` / "EUR/USD 3-Month Forward" — the row is an `fx_forward` and its fail reason is `funding_gap`, so a currency-forward identity is the coherent one (canon: instrument type and fail reason must cohere). **TotalEnergies now exists nowhere in the data**, so the mockup asserts an identity that cannot be rendered. Update in the pre-Phase-4 mockup pass.
- [ ] **Mockup currency label USD → EUR on the hero trade** (both mockups), in the pre-Phase-4 pass. The hero is authored in EUR so ticker, ISIN, CSD and the CSDR penalty agree; the mockups currently show "18.7M USD" alongside a EUR penalty accrual. Banked figure 18.7M is unchanged — only the currency label moves.

## Blocking

- [ ] **BUILD_LOG header checkpoint line is stale** — *next session that touches promotion.* Line 5 reads "current through 2026-09-01 (v5 verification)" while per-entry checkpoints continued through 2026-09-11 and the 2026-09-21 retrofit entry stages five candidates. §9.5 says such a session cannot call itself complete until the header is reconciled against the staging section. Not moved at the retrofit, because moving it asserts a reconciliation that was not done.

- [ ] **Phase 5 demo-script decision — the opening state of the book.** Two legitimate stagings and they tell different stories: **(a) empty until Simulate**, so the feed arriving is the whole Act 1 beat and the queue fills from nothing on the 30-second refresh; **(b) pre-populated**, so the analyst opens onto a working book and Simulate adds to it, which is more realistic but makes the refresh beat subtler. Decide when the demo script is written, not before — it changes what `Reset Demo` leaves behind and what `Verify Ready` asserts.

- ✅ 2026-09-09 — **Settles/cutoff coherence RULED AND SHIPPED.** Option (a) taken: `Settles` derives from `date(cutoffTs)` — the cutoff *is* the settlement-date cutoff, so the two can never contradict each other, no Snowflake baseline is mutated, and the value is Appian-computed (correctly unmarked). Rebuilt from the cutoff's LOCAL rendering, since `text()` renders local while date-part functions return UTC (§4). Verified on four renders: "Settles Wed 9 Sep" against same-day cutoffs, where it previously read "Thu 2 Jan".
- [ ] **Fixture prose is COMPOSED from the row, never authored beside it.** Standing rule from the 3a finding (2026-09-08): the P4-VERIFY assessments had been hand-written with invented figures — one said "26.1 hours" where the trade carries 3.0 — so the rail and the assessment on the same case disagreed and the screen showed both faithfully. All four are now rewritten from stored values. Apply to every fixture authored from here: any narrative in demo data is built from the record it describes, or it is a forgery with a friendly face.

- ✅ 2026-09-11 — **REJECTED: dedicated service account / `SO Integration Services` group.** Why: the PAT in the connected system already is the service credential. (Ruled in Part B2; the only identity change is Scott's SO Demo Admins viewer grants.)
- [ ] **NEVER SURFACE `RunSummary` — reinstated 2026-09-01, and the earlier relaxation was WRONG.** The v4 result (two clean summaries) did not replicate: both v5 runs fabricated with the same Run summary instruction in place. One invented a customer, **an account number and a $15,847.32 balance** from an entirely different domain; the other invented a non-existent remediation rule, a "regulatory cutoff", and **reported confidence 0.85 when the actual output was 0.45**. **Now TEN observations across four instruction states** (2026-09-02 added six: invented trade ids `T-2024-03-15-0042` / `TRD-2024-ETF-7891`, an invented `CUSIP 912810TW8, quantity 50,000, price $98.50`, a WRONG ISIN for SAP — `DE0005140008` where the context supplied `DE0007164600` — invented dates, an invented four-step remediation procedure, and one run **reporting confidence 0.85 when the actual output was 0.88**; several drifted into meta-narration, "I'll help you execute the agent task"). The field is not steerable. Keep it out of every UI, audit view, operator-facing text, and diagnostic — it is captured to `pv!runSummary` and consumed by nothing; keep it that way.
- [ ] **Demo-design constraint on the straight-through beat — LARGELY DISSOLVED 2026-09-02 by the authored-hero decision.** Packet trades are authored, so any probability/counterparty combination the story needs can simply be written (see `hero-trade-spec.md`); this constraint now binds only beats that must use a *baseline* trade. Kept for that case. Original finding: No trade in the dataset has fail probability > 0.80 with a non-high-risk counterparty (query returned 0 matches; max probability with a non-high counterparty is 0.6908, and no "low" tier appears in the top 12). The scoring heuristic makes counterparty risk dominant. A high-probability *and* clean-counterparty case cannot be staged from this data — the straight-through beat must either use a mid-probability trade or the Phase 8 model rescoring must change the relationship.
- [ ] **Agent latency ~44s per case (42–47s measured 2026-09-02 across five runs under v6 with the extended context; was 39–45s under v5).** The richer context did not materially slow it. Still far too slow for a synchronous screen action — Phase 4 must triage on arrival or fire-and-poll, not on click. Decide the pattern before building the watchlist.

- ✅ **Permanent network policy (Daniel)** — NY↔Snowflake still on a 24h bypass; everything integration-dependent fails when it lapses. Resolved in loose form; tighten to Appian egress IPs = one VALUE_LIST edit. This is production-hygiene item, not a blocker

## Before demo

- [ ] **Persona sail sessions** — *Scott.* The retrofit's core (§4, §6) moves persona content/state/behaviour checks to sail, which needs `alex.analyst` and `sam.supervisor` to be local-password accounts, each logged in once into `~/.sail-alex.analyst` / `~/.sail-sam.supervisor` (`GETTING_STARTED.md` §1, "once per persona"). Not yet recorded whether either is local-password. Trigger: the first build prompt that ends with persona-scoped sail steps; until then those checks stay on the browser list.

- ✅ 2026-08-31 — `left(..., 255)` caps raised to 4000 across all six models (SO_triageCase ×4 details, SO_createTriageCase, SO_assignCase, SO_addCommentCase, SO_escalateCase, SO_recordDispositionCase); each confirmed by readback.

- ✅ 2026-09-01 — Straight-through lane wired to live agent output; all three lanes verified against real agent values, not stubs (v5: clean 0.87 → Straight-Through, concerning 0.45 → Escalated, credit case escalates on reason).
- [ ] **Trace review of the live agent against `agent/eval.md` section D** (≤2 calls standard, ≤1 analyst call, no corroboration call) — the staged tool-economy candidate's trial. **Requires Agent Studio's monitor view: tool calls are NOT observable from a process run, so this cannot be closed from a Dev MCP session — it is Scott's.**
- [ ] Residual-quantity disposition decision (a partial settle leaving quantity open currently reads as a clean close).

## Done

- ✅ 2026-09-02 — **PART E PASSES. v6 shipped; the straddle is gone.** Five runs, three specimens, through the dev MCP as `scott.thorn`: clean TRD016924 **0.87 / 0.88 / 0.88** → Straight-Through ×3; concerning TRD044567 **0.35** → Escalated; hero TRD9NY101 **0.62** → Pending Analyst with the timing arithmetic stated. Spread across the clean runs **0.01**, against 0.09 under v5. Full detail in `agent/eval.md` v6.
- ✅ 2026-09-02 — **The v5 "regression" was VARIANCE, not regression** — same specimen returned 0.78 and 0.87 six minutes apart, straddling the 0.80 gate. Caught by running a replicate before acting. v5 was also resting on a false premise (it called Zephyr's 9.55% "below-average"; the 50-counterparty mean is **9.20%**), which is why it was never stable. v6 keys the rule to risk tier with a 13.8% backstop, in the empty gap between the medium and high bands.
- ✅ 2026-09-02 — **Designer column additions done by Scott** (direct data access, no sync): ISIN/CSD on SO Instrument (TICKER_ORIG/NAME_ORIG correctly excluded), QUANTITY/IS_MATCHED/MATCHED_AT on SO Trade, both ops-contact fields on SO Counterparty, COUNTERPARTY_ID on SO Settlement History plus its N:1 to SO Counterparty. `SO_caseContext` extended to seven lines and verified by render.
- ✅ 2026-09-02 — **CTX-TEST cleanup verified at zero.** Cases 27–35, 9 comments, 27 event rows deleted; all three tables read back **0** as a supervisor-scoped account (§5). Throwaway `SO_zz_probeTrades` deleted; five real expression rules remain.
- ✅ 2026-09-02 — **Part C-11/12 built and break-tested.** `SO_PENALTY_BP_PER_DAY` = 1.0 (readback `"value":1`, unquoted — real number, §1). `SO_penaltyAccrual`: 18,700,500 EUR → daily **1,870.05 / "~1.9K EUR / day"**, five-day **9,350.25 / "~9.4K EUR"** — both matching the mockup; zero and null both yield 0 without erroring; M-branch verified at 2.34bn → "~1.2M USD". `SO_onwardDeliveries`: **1** with a known onward sell, **0** without, **0** for the hero instrument INS0147 at 2026-09-02 (so the hero reads "contained", as the mockup shows), **0** on both null guards.
- ✅ 2026-09-02 — **Part D: `SO_caseContext` extended** with an `INSTRUMENT:` line (ticker — name) and an `ONWARD DELIVERIES:` plain statement, plus thousands-separated notional. Verified by render on a throwaway case: `INSTRUMENT: BAS GY — BASF SE | instrument id INS0078`, `notional 88,001.91 EUR`, `ONWARD DELIVERIES: no onward deliveries … contained to this settlement`. Check case and its event row deleted. ISIN / venue / matched pending the Designer re-sync above.
- ✅ 2026-09-02 — **`SO Settlement History` → `SO Trade` ONE_TO_ONE relationship built** (`68a4ae92-1d2c-4dc1-8943-5dfcb1875738`), giving the recent-fails view its one-hop path to instrument ticker and notional. Both hand-built record types verified: correct source table and data source, faithful field types, no record events, no related actions, security consistent with the app's other non-anchor types.
- ✅ 2026-09-02 — **XOR context guard built and break-tested** (`SO_triageCase` node 8, "Context resolved?"). Node 9 → node 8 → agent only when `SO Settlement Case` resolves a `tradeID` for `pv!caseId`; otherwise straight to End. Break-test through the dev MCP (`testProcessModel`, caseId 999999): COMPLETED in **4.2s**, `agentOutputs: null`, `runId: null`, and audit/case/comment counts **unchanged at 6/2/2** — zero agent invocation, zero writes. Normal path (caseId 25, TRD016924) then ran clean: **confidence 0.87, straight-through**, reproducing the v5 figure exactly, +2 events +1 comment written correctly.
- ✅ 2026-09-02 — **Two-server identity trap identified and closed.** `appian_*` runtime tools execute as `scott.mcp` (Starwood service account, no scope here); dev-MCP tools execute as `scott.thorn`. Both share the `mcp__appian__` namespace. Runtime tools now BANNED in CLAUDE.md; identity map recorded in CLAUDE.md + deploy-notes.
- ✅ 2026-09-02 — **CTX-TEST cleanup verified at zero.** Cases 24/25, 3 comments and 8 event rows deleted; all three tables read back **0** as the Dev MCP design account (SO Supervisors, full scope), so zero means zero and not a row-security artifact (§5).
- ✅ 2026-09-02 — **CANON AHEAD OF BUILD CLEARED.** `SO_remediationForReason` edited: `insufficient_securities` now returns **"Arrange cover borrow / partial release"** (was "Initiate borrow / partial release"). Verified by live `testRule` on all five branches — the other three pairs and the Manual review fallback are byte-identical to before. Canon and build agree again.
- ✅ 2026-09-02 — **Dev MCP design-object surface RESTORED.** The 2026-09-01 outage did not recur: `listRecordTypes`, `getRecordType`, `createExpressionRule`, `updateExpressionRule`, `testRule` all exercised successfully this session. No action taken to fix it; it came back on its own, so treat a missing design surface as transient-and-retryable rather than as a standing block.

- ✅ 2026-09-01 — ~~Threshold decision CLOSED (ceiling re-probe)~~ **SUPERSEDED — v4 subsequently dropped the clean specimen to 0.78 and reopened this; see the v5 entry below for the standing decision.** Kept for history: excluding fail probability moved TRD016924 from 0.72 → 0.85 and straight-through fired for the first time.
- ✅ 2026-09-01 — **Discrimination PROVEN under v4.** Same specimen pair, facts the only variable: clean **0.78**, concerning **0.35** — spread **0.43**, where v3 gave 0.85/0.85. Both assessments state the timing arithmetic in numbers, which was the pass condition; the concerning case reads *"26.07 hours exceeds the 23 hours remaining… the remediation window is already breached."* The Phase 5 discrimination blocker is cleared.
- ✅ 2026-09-01 — **Threshold decision CLOSED: `SO_CONFIDENCE_THRESHOLD` stays 0.80**, a bank-realistic bar, deliberately not tuned to the agent. v5's step-3 rewrite (only genuinely concerning facts count; favorable facts are reassurance) lifted the clean specimen 0.78 → **0.87** while the concerning specimen went 0.35 → **0.45**. Both directions correct at the unchanged threshold. **Phase 5 packet hold LIFTED.**
- ✅ 2026-09-01 — **Escalate event detail defect FIXED** (node 23). Composed at write time from values in hand: counterparty_default keeps the credit sentence, any other reason renders "Escalated by agent: <failReason> — <first sentence of assessment>". Verified live: a timing-escalated funding_gap now stores the lag-vs-window reason, not a false credit rationale.
- ✅ 2026-09-01 — **SO Triage Agent built, wired and verified live.** Agent `126a096b-787c-400c-ba21-6498d3ba0a0e`; `AGENT_NODE_PLACEHOLDER` replaced by the Execute AI Agent node (input `caseContext`); definition iterated v1→v5 and both directions verified at threshold 0.80.
- ✅ 2026-08-28 — Phase 2 complete server-side: case record type, comment + record events, both expression rules, four related actions, event-write gating proven by break-test.
- ✅ 2026-09-21 — **Repo retrofit** to appian-devmcp-method conventions; see the BUILD_LOG entry of that date.

## Deferred (retrofit, 2026-09-21)

- [ ] **Dev MCP plugin 26.6.95 is on the App Market; NY runs 26.6.90** — *Scott / site admin.* `getDevMcpVersionInfo` 2026-09-21. The server and bundle match the `reference/toolchain.md` §1 pin, so there is no drift today. Trigger: a quiet day between phases, never mid-phase — the admin upgrades the plugin, then "run the update procedure" (`maintenance/dev-mcp-update.md`).
