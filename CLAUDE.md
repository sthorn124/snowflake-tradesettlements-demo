# Appian Dev MCP build — operating core

This file is the generic operating core for an Appian application built through the Appian Dev MCP from Claude Code. Copy it into a new build's project folder as `CLAUDE.md`, keep everything below as written, and add the project's own sections beneath it (vocabulary canon, data model, naming prefix, groups, business rules, repeatability rules, files in the repo). Nothing in this core names a client, an application, a record type, a dataset, or a demo scenario; everything in it applies to every build. Bracketed S-numbers cite rows of the sail evaluation's measurement record in `examples/persona-verification-walkthrough.md`.

**Skill precedence.** The user-level **appian-supplemental** skill governs environment, platform, and Dev MCP facts and the portable working method. A project skill (a `<prefix>-standards` or client-standards skill), if the build has one, governs SAIL styling, naming, colours, and this build's layout applications. This file governs the project's data model, vocabulary, and business rules and wins on conflict for project matters. appian-supplemental wins over the vendor pack and the docs only where it records a measured correction. Surprising environment behaviour discovered in a build is recorded in the build log as a promotion candidate first; it reaches appian-supplemental only through the promotion gate (§9), and the project file then keeps at most an application pointer.

## 1. Grounding order at session start

Read, in this order, before anything else:

1. **This file** (`CLAUDE.md`), in full.
2. **The client-standards / project skill**, if the build has one.
3. **The appian-supplemental skill** (user level) — mandatory before any Appian object work or any SAIL generation or edit. Load the vendor pack's Dev MCP references it names before object work.
4. **The BUILD_LOG tail** — the last session entry at minimum (roughly the last 50 lines), including the promotion checkpoint line — then the open sections of `TODO.md` and the current phase of `BUILD_PLAN.md`.

Then run the preflight (§2). Nothing is designed, written, or run before all four are read: the log tail is where the previous session left the environment, and the environment is never assumed to match the plan.

## 2. Session preflight (before any work)

1. **Confirm the build is planned.** `BUILD_PLAN.md` must exist and contain actual build content beyond the template stub: the stub marker line is gone and the Build Phases section holds at least one checklist item. If it does not, **STOP**: report that the build is not yet planned and that Phase 0 — the build plan, the demo narrative, the personas, and the entity-level data model — happens in the claude.ai Project before build sessions begin (see `GETTING_STARTED.md` §3). In this state do not create objects, seed data, or accept build prompts; the only permitted work is reading the environment.
2. **Verify the Dev MCP is connected AND the full design-object tool surface is present.** List the tools available in the session and confirm the design CRUD families are loaded — record types, fields, relationships, expression rules, interfaces, process models and nodes, sites, constants, documents, `testRule`, `testInterface`, `testProcessModel`, `validateDesignObject` — not merely `ping` or a runtime subset. Then make one trivial design read, `listRecordTypes` scoped to the **Application UUID** build parameter (§13), and confirm it returns the application's types.
3. **If the design tools are missing or the read fails: STOP.** Report the tool count and which families are present. Do not diagnose the cause, do not improvise a reduced-scope pass, do not substitute runtime tools, do not proceed degraded. A missing design surface has been measured as transient (it returned on its own the next session), so treat it as retry-next-session, not as something to work around. The one exception is a name collision, which does not clear on retry: if the tools that are present under `appian` are the runtime family (snake_case `appian_*` plus `ping`), report that a runtime server shares the Dev MCP's name (§6, `reference/toolchain.md` §2) [S34]. Note that `claude mcp list` reporting the server connected and `ping` returning 200 prove transport, not that the design surface loaded — in a collision, the `ping` that answers is the runtime server's.
4. **Check the Dev MCP and sail versions against their pins — a flag, not a gate.** Call `getDevMcpVersionInfo` and compare the reported plugin version and build stamp with the version pin at the top of `reference/toolchain.md` §1. On a mismatch: report that the server has updated and that `toolchain.md` §1 describes the previous generation; offer to walk the operator through the update procedure (they say "run the update procedure" and the session executes `maintenance/dev-mcp-update.md`); print both bundle download paths for the current site, with `<site>` taken from the registration's `LCP_URL`: the documented downloads page `https://<site>/suite/plugins/servlet/stateless/downloads` (the operator-facing entry), and the direct bundle link the version tool itself prints, `https://<site>/suite/plugins/servlet/stateless/lcp-mcp-bundle` (`maintenance/dev-mcp-update.md` Step 0); and note the drift in `BUILD_LOG.md`. Also surface the tool's own recommendations: if the site's plugin is behind the App Market, say so and note that the site admin updates the plugin before a bundle download is worthwhile, because the bundle versions with the site's plugin. Then run `sail --version` and report it beside the Dev MCP's version, compared with the sail pin in `reference/toolchain.md` §12. sail ships in the same bundle and reported the bundle's own version on the verified machine [S1]. If `sail` is missing or does not execute, say so, point the operator at `GETTING_STARTED.md` §1, and route persona-scoped checks to the browser checklist until it is installed. Unlike the plan gate, the session proceeds after reporting.
5. **Establish which MCP server every tool family belongs to and which identity it executes as** (§6). Servers are named by role: `appian` for the Dev MCP, `appian-runtime` for a runtime server. Two servers under one name shadow or merge silently [S34]:
   - **Merge:** snake_case `appian_*` tools under `mcp__appian__`.
   - **Shadow:** a connected `appian` missing the design families.
6. **Compare the repo's `skills/appian-supplemental/SKILL.md` with the installed user-level copy at `~/.claude/skills/appian-supplemental/SKILL.md`.** If they differ, say so, show which is newer, and let the human decide the direction of the sync before continuing: install the repo copy after a template update, or copy the installed skill into the repo after a promotion. Never overwrite either silently.
7. **Run the per-session ritual named in the Per-session ritual build parameter** (§13) before rendering anything — for example re-dating time-anchored fixtures with the project's idempotent script, so a stale book is not mistaken for a broken screen.
8. State the executing identity and its group scope at the top of the session's work, because every readback that follows is interpreted under it (§4). For sail, the persona sessions available on this machine are the ones step 9 reports; no sail command reports the acting identity (§6) [S2]. Compare the executing identity with the **Design account** build parameter and report a mismatch. Verify by readback, not from the plan, that the groups named in the **Security groups** build parameter exist, are nested as believed, and have members — a plan has recorded groups as nested that were siblings, and as created when they were not.

9. **Report the persona sail sessions on this machine.** Persona sessions are machine-local state. They are never written to a tracked file and never reconciled against the plan.
   - **Discover.** Take every directory matching `~/.sail-*`, plus the default `~/.sail` if it holds a `session.json`.
   - **Read each `session.json` for its `username` and `host` fields only.** The file is a bearer credential [S27], so it is never printed or copied into the session, the log, or `Closeout.md`. Read the two fields with a one-line extractor, never with `cat`. Where the file carries `"source": "devmcp"`, the directory is a designer import, not a persona, and is reported as such [S30]. A directory without a `session.json` is reported as holding no session.
   - **Check liveness, read-only:** `sail --data-dir <dir> pages <site>`, where `<site>` is the **Persona site stub** build parameter (§13).
   - **Report:** each username as **live** (the page list came back) or **expired** (an authentication error), quoting sail's error for anything else. Name the directory beside each result. If the default `~/.sail` holds a session, say so, because it should stay empty (§6).
   - **That is the whole step.** Nothing is logged in, repaired, or recorded.
   - **The username is only a claim.** It is what the operator typed at login, not something the server verified. Liveness proves the session works; which account it is still gets confirmed by observation, as §6 requires.
   - **Later in the session:** when a build prompt asks for verification as a persona that has no live session, sail's own error is the signal. Report "no live session for `<persona>`, run the login" and skip that verification. Never fall back to another identity.

## 3. Observation precedes fixes

- **Never guess before reading evidence.** Before theorising about a defect, read the deployed object, the stored expression (read back, not what was sent), the process instance's variables, the node's actual configuration, or the row itself. The reported symptom is frequently not the defect; the deployed source frequently is not what the last session believed it deployed.
- **Reproduce before changing.** Reproduce the failure, change one thing, re-measure. A control on data known to be good, or on a path known to work, is what isolates a cause; without it a fix is a guess that happened to coincide.
- **A single wrong-side reading is one draw, not a verdict.** Replicate before acting; an apparent regression has been measured as variance on a held-constant specimen.
- **A negative capability claim names its evidence** — "tried here, failed, here is the error" — never "the docs do not mention it". A capability is present if something on the instance uses it; find the working example and copy its configuration before concluding anything cannot be done.
- **Stop and report rather than improvise** when a capability is missing, when an input contract cannot be read (an agent's declared input names, a node's parameter names), or when the only next step is guessing spellings. Reporting a blocker is a complete outcome; a workaround invented under uncertainty is not.
- **Read the docs, then the instance, in that order — and the instance wins.** Docs establish what a parameter is; only the instance establishes what this environment does.

## 4. Verification doctrine

- **Writes are verified by existence and readback, never by operation status.** `COMPLETED`, HTTP 200, `error: null`, a green save and "valid" prove nothing happened. A write path is proven by exercising the actual control and counting persisted rows per record type; a delete is proven by absence (404 / zero rows) read as a full-scope account; a document is proven by existence and byte size. Direct payload inserts validate shape and bypass the control's accumulator, so they are not verification of the control.
- **Object validator over `validateExpression`.** `validateExpression` evaluates both branches of an `if`, is not a keyword check, and accepts parameters the object validator rejects; the rendered tree lists attributes that are not settable. Only the object validator (`validateDesignObject`, `createInterface`/`updateInterface`, `createExpressionRule`) is authoritative for interfaces and rules, and an interface counts as done only at `diagnostics.error: null` through `testInterface`.
- **Content, state, and behaviour are verified from the terminal, per persona, through sail; geometry and paint only in the browser.**
  - **What sail verifies.** The sail CLI (`reference/toolchain.md` §12) reads and drives published pages as whichever account its session holds [S5, S10–S13, S15, S16, S21]:
    - content, in page order;
    - field state: required, invalid, disabled, read-only;
    - visibility and conditionally rendered content, such as status-driven bands;
    - navigation through record links, tabs, related actions, and process launches;
    - behaviour: cascades, validation, and submits.
  - **Read the YAML, not just the listing.** The listing sail prints is a summary. Full grid rows, tag values, milestone details, and a changed sort order exist only in its stored YAML [S7].
  - **What sail cannot verify.** The YAML carries style values (hex colours, size and style enums) and the requested layout parameters. It carries nothing computed: no pixel sizes, positions, line breaks, or truncation [S8]. So wrapping, truncation, overflow, alignment, card heights, focus, hover, link colour against the site accent, chart drawing, and branding are never sail-verifiable. The Dev MCP's tree and harness renders cannot verify them either: they prove structure (component order, colour values, requested widths, error-free evaluation) and carry no geometry.
  - **Reporting.** Every UI summary separates terminal-verified facts, each with the account it ran as, from browser-only ones. It never describes a layout as confirmed on terminal evidence. Where a parameter's whole job is geometry, verify the parameter's value, not the shape of the content it governs.
- **A write driven through sail is verified by a fresh read of its target, never by the submit output.**
  - The submit prints the page as it was before the form opened, so the old value is still on screen after a successful write [S13].
  - The reload its own note suggests, `load --fresh`, fetches the site's default page, not the record [S13].
  - Verify by navigating to the specific page or record afterwards and, where the app has one, by its own attribution: an activity entry naming the acting user [S13].
  - A submit that validation blocked exits 0 with `✓ Interacted` [S12]. Read the ⚠ lines and the field states, never the exit code.
- **Build prompts may end with persona-scoped verification steps** — "as `<persona>` via sail: these pages resolve, this band renders, this action appears only when …" — instead of flagging those facts for browser verification; each of those three kinds of fact was read from the terminal as a persona [S4, S5, S10]. The session runs them against the operator's persona sessions and reports each result with its account. Geometry still goes to the browser checklist.
- **Readbacks must state the identity and scope they ran under, or they prove nothing.** Record-level security filters every read silently — `listRecordData`, `a!queryRecordType`, counts, min and max, related-record traversals — with no marker distinguishing absent from invisible. Write the scope into the readback ("as `<user>`, member of `<groups>`"). Before concluding rows are absent, re-read as a member of every scope or prove absence another way. A design-account render proves nothing about a persona: on one site, the design account and a persona disagreed in both directions on pages, KPI values, masked values, and available actions [S32].
- **Readbacks both over-report and under-report schema; measure behaviour, never trust metadata.** A column width has read back wider than the physical column (after an ALTER that changed nothing and returned 200) and narrower than it (a 4000-character column reported as 255). Document metadata reports an extension the runtime resolves differently. A create response has omitted relationships that exist. An integration reads back with function names that do not match what runs, and a boolean set to `false` reads back `null`. The rule is the same in every case: probe through the path production uses — a write of the real length through the real Write Records node, a runtime `document()` call, one live integration call — and treat a readback as a claim to be tested.
- **Visibility, security, and document access are verified behaviourally**, as a member and as a non-member: through sail as each persona for pages, content, values, and actions [S32], and in the browser for document access, which sail cannot reach [S9]. Nothing automated distinguishes "hidden by rule" from "hidden by a broken rule", and a visibility expression that throws hides its target without reporting.
- **A hidden branch is unevaluated, so a clean render proves nothing about it.** Anything behind `showWhen: false`, an inactive wizard step, a toggled section, or a view-switch local runs untested until it is revealed. Flip the default in a probe copy, render, restore, verify the restored source byte-identical, and re-render.
- **Gates are proven by break-test, not by a happy path.** Force the upstream failure and confirm the guarded node did not fire; a passing happy path says nothing about a gate.
- **A generated harness goes stale silently and positively.** Regenerate it in the same change that edits its source, and before trusting a harness render check that one string you know you changed appears in it.
- **Every summary lists what was NOT verified and why**, with the browser checklist the human can run for what stays browser-only — steps, personas, expected strings. Partial runs are reported as partial, never as a path exercised.
- **Verification ceremony scope:** these are build-time and change-time rules. Routine operation of a working build needs no readback, no row count, no wrapper; ceremony retires with the defect it was built for.

## 5. Docs-search consultation

The docs-search MCP is the first resort for any uncertain platform semantics — function signatures, parameter vocabularies, node behaviour, configuration options, anywhere in the platform. Never guess at a keyword, parameter value, or documented behaviour when the docs can settle it; state the documented answer before acting on it.

Layout and visual edits carry a mandatory gate: before any edit that creates or modifies interface layout, styling, or visual design — including a one-line width or colour change — consult docs-search on the parameter semantics in play (width vocabulary of the specific layout, wrapping, alignment, component interaction) together with the vendor pack's layout references the supplemental names. This area is gated because its failures are silent: keywords no-op in validators that don't reject them, a plausible invalid icon fails the whole interface at create time, and "it renders in the component tree" is not evidence a keyword is accepted.

When the docs MCP is unavailable (its session expires), say so, and settle the question by measurement on the instance, which outranks documentation in the supplemental's precedence order anyway.

## 6. Identity discipline

- **Know which MCP server and which identity every tool call runs as.** A tool's name announces its server only through the name the server was registered under, and the client's server listing may not show every server.
  - **Name servers by role:** `appian` for the Dev MCP (the name in Appian's documented registration, `reference/toolchain.md` §1) and `appian-runtime` for a runtime server.
  - **Two servers under one name fail silently in either direction** [S34]. One session carried only the desktop-inherited server's 10 tools under the name, with the design tools absent. Another carried both sets merged: 167 tools under one prefix.
  - **How each is caught.** The preflight's design-surface check (§2) exists for the first case. The tool-family tell catches the second: camelCase unprefixed design tools versus snake_case, product-prefixed runtime tools plus `ping`.
  - Confirm the executing identity when in doubt with a throwaway rule returning `loggedInUser()`, then delete it.
- **Design work goes through the Dev MCP as the designer account.** Processes are driven through `testProcessModel`; reads through `testRule` or wrapper interfaces. A runtime or service-account MCP never executes design-session work — not for tests, not for break-tests, not "just to check" — and the service account is granted no group membership or scope in the application.
- **Why this is binding:** under a foreign identity, row-secured reads return empty silently while primary-key-targeted writes still succeed. A process completes green having read nothing, and anything downstream that reasons over the empty read — an LLM especially — confabulates rather than fails, writing plausible rows into the audit trail. The tell is that text composed from the process's own parameters is correct while every record read is blank.
- **Connected systems and integrations run under their own configured credential, not the session's**, and a session cannot always see which. State which identity a probe or a process ran under, and never assume a connected system's scope from the design account's.
- **sail is a third identity path.** Beside the Dev MCP (the designer) and a runtime MCP (a service account), the sail CLI drives published pages as whichever account its session holds.
  - **Where its identity lives:**
    - It is whichever `session.json` sits in the data directory in effect [S25, S26].
    - That file is a portable bearer credential: a copy authenticated from another directory [S27].
    - No sail command prints the acting identity, and the file's `username` is only the name typed at login [S2, S25].
    - Persona-scoped reads look like plain facts: the same KPI read 94.41 as a persona and 126.01 as the designer, with nothing on screen saying which [S32].
  - **Therefore:**
    - Every sail observation states its account ("as `<persona>` via sail").
    - Each persona has its own data directory, passed on every command (`--data-dir ~/.sail-<persona>`).
    - The default directory stays empty, so a command that forgets the flag fails with "no session found" instead of running as someone [S26].
    - Identity is confirmed by observation — the pages that resolve [S4], an activity entry naming the acting user after a write [S13] — never assumed from a directory's name.
- **Persona logins are the operator's.** sail's password login reads `SAIL_USERNAME` and `SAIL_PASSWORD` from the environment and, per its help, works only for local Appian accounts. The operator logs each persona in once, and the session persists on disk [S25, S29]. A session never asks for, types, or stores a password. When a persona's session is missing or rejected, it reports "no live session for `<persona>`, run the login" and skips that verification, with no fallback to another identity (§2, step 9).
- **`--from-devmcp` is reserved for deliberate designer-versus-persona diffs, never routine verification.**
  - It imports the designer's session, so every read through it has the design account's scope [S32].
  - The import is not a copy of that session. All three cookie values are the Dev MCP's own, and `sail logout` on the import revoked the Dev MCP's server session as well [S31].
  - Import into a throwaway data directory, and discard it by deleting that directory, never by `logout`.
- **The design account is usually full-scope**, so a design-account render, count, or click proves nothing about what a persona sees [S32]. Persona checks run through sail as the persona, from sessions the operator logged in, for content, state, and behaviour. Geometry, and whatever sail cannot reach, stay browser checks owned by a human (§4).

## 7. The BUILD_LOG contract

- **Append-only session log** of what has actually been built in the environment: object names and UUIDs, versions, the decisions behind them and why, what was verified and how (with the account scope of every readback), what was not verified, and findings. Append after every build step. Corrections are new entries that name what they correct; superseded candidates in the staging section carry a bracketed in-place marker (`[RESOLVED …]`, `[REVERSED …]`, `[TRIAL REASSIGNED …]`) so a reader of that section alone never chases a dead trigger.
- **The readback is the record.** Object versions, states, and security are written from readback at close-out, never from memory; a close-out has recorded the wrong version by remembering it.
- **The tail provides continuity and is always consulted before acting** (§1). The log is the environment's state of record between sessions; `BUILD_PLAN.md` is the intent.
- **It is the staging area for promotion candidates** (§9): it carries the "Promotion candidates (staging)" section, and every entry that touches promotion ends with a promotion checkpoint.
- **Entry shape:** date and title; scope line (identity and groups every readback ran under, including the account behind each sail data directory); what changed, by object; decisions and why; verified (how, with counts and scope); not verified (and why, with the browser checklist); promotion checkpoint.
- **Structural deltas are logged like data deltas.** When a built layout departs from its mockup, say so in the log and fix the mockup in the next mockup pass, or the mockup and the build drift and neither is trustworthy.

## 8. The TODO.md contract

- **Open items by class.** The sections a build carries are: Blocking; Before demo; Browser checks owed (each with a human owner and the exact steps, personas, and expected strings); Client validation questions; Deferred (each with its trigger); Done (✅ with the date). Phase-specific sections may sit above these while a phase is live.
- **Sessions add discovered items unprompted as they surface** — deferred work, client questions, browser checks a session cannot perform, edge cases observed but not fixed — and any response that touched `TODO.md` ends with a `TODO changes:` line stating what was added, moved, or closed.
- **Every item carries an owner and a firing condition.** Parked work names the defined moment that fires it ("trigger: the next session that opens this object", "before another presenter runs it"); nothing is parked on "later".
- **Superseded items are struck through with a pointer to the standing decision**, not deleted, when the reasoning is worth keeping; completed items move to Done with the date. Retired mechanisms keep their numbering so references elsewhere still resolve.
- **A browser check a session cannot do is written as a checklist the human can run**, not as an open question.

## 9. The promotion loop

`BUILD_LOG findings → promotion candidates with named triggers → gate evaluation at close-out → promotion into CLAUDE.md or skill files`, with a checkpoint that makes it self-enforcing.

1. **Capture.** A surprising observation is recorded in the build log as a candidate the session it is found — stated as the trap and the working form, marked STAGED with the gate it is held at (usually gate 1, one observation) and a **named trigger**: the defined moment that will confirm or discard it ("the next process model with a datetime parameter", "the first traversal verified in a row-secured app", "any session where a scoped read returns empty against data known to exist").
2. **Gate.** An entry is promoted only when all five hold: **measured**, not suspected (reproduction, a control, or a deliberate re-test — two observations in one direction are not reproduction); **survives the noun test** (stateable with zero project nouns); **earns its context cost** (contradicts the docs or the pack, or cost real time); **rule-shaped** (trap and working form, instance-dependent facts tagged "re-verify per instance"); **contradictions named** (which docs page or pack file it supersedes).
3. **Home.** Portable platform and Dev MCP facts go to appian-supplemental; project facts to the project's CLAUDE.md; styling to the project skill; prompt-design and method observations that fail the noun test go to the project's agent eval log or CLAUDE.md. One home per fact; the project file keeps at most an application pointer to a promoted entry.
4. **Close-out line.** Every close-out includes "Promotion candidates: N found; promoted / listed / none", so the session that discovers a finding classifies it while it is in context.
5. **Checkpoint.** The build log carries "Promotion checkpoint: current through `<entry>`". A session that touches promotion refuses to call itself complete if the checkpoint lags the log tail by more than one session.
6. **Trigger firing.** When a trigger fires, the candidate is ruled — promoted, discarded with the gate it failed, or re-staged with a new trigger — and the ruling is written into the staging section with a bracketed marker on the old entry. A candidate closed on insufficient evidence is reopened, and the reversal is recorded rather than the history rewritten.

## 10. The close-out routine

The final actions of every session, in this order:

1. **Write `Closeout.md`** — a full, dynamic write-out of everything that happened in the run, rewritten from scratch every session in one write: no fixed template, no growth, never patched, never appended to. Its first line is the session-date header `# Closeout — <YYYY-MM-DD> — <one-line title>`, so the Project can tell from the first line whether the file it fetched describes the session it expects; everything below the header follows the session. The file and the close-out printed in the session are the same document; if the session ran through several rounds, the close-out is written from the current state of the work, not accreted round by round. Its content follows the session — typically scope and identity, what changed by object, how it works, verified and not verified, the browser checklist, rulings needed, promotion candidates, TODO changes, `BUILD_PLAN.md` changes.
2. **Update `BUILD_PLAN.md`** — mark items completed this session (`- [ ]` becomes `- ✅ <date>`) and add newly discovered tasks to the right phase. A close-out that built something but did not touch the plan is incomplete. The plan stays a high-level checklist; detailed specs live in the build prompts.
3. **Append the BUILD_LOG entry** (§7), ending with the promotion checkpoint.
4. **Evaluate promotion candidates** (§9) and write the promotion line. If a promotion changed the installed user-level skill, copy it into the repo's `skills/appian-supplemental/SKILL.md` so the machine and the repo agree and the template pull request can be raised from the build repo.
5. **Update `TODO.md`** (§8) and print the `TODO changes:` line.
6. **Commit and push:** `git add -A`, `git commit -m "closeout: <date> — <one-line summary>"`, `git push origin main`.
7. **Verify the push landed.** `git fetch origin`, then confirm `git rev-parse HEAD` equals `git rev-parse origin/main` and `git status` reports nothing to commit. A push exit code is operation status, not evidence (§4): the next conversation reads `Closeout.md` from GitHub, so a close-out whose commit is not on `origin/main` has not been handed off. On a mismatch or a rejected push, report it as a failed close-out with the git output; never force-push to make it agree.

## 11. Regression discipline

Worked examples: `examples/agent-eval-walkthrough.md` (the specimen discipline) and `examples/deterministic-validation-gauntlet.md` (the validation gate that makes agent output safe to act on).

- **Hold named test specimens constant across iterations**, so a behaviour change is attributable to the change made and not to input drift. Specimens are identified by their properties, never by platform-assigned ids, which change on relaunch. The specimen set includes a clean case and a concerning case; every tuning change is probed in both directions, because a clean-case pass alone cannot distinguish a working definition from one that says yes to everything.
- **Change one thing at a time**, state what the variable was, and replicate before acting on a single wrong-side reading (§3). When two changes go in together, record that the decisive one is not isolated rather than attributing it.
- **Pass conditions are stated before the run**, in terms the output must contain (the arithmetic stated number against number, the row written, the event absent), never in terms of a score that could be reached by luck.
- **Break-tests are part of the suite**: the forced failure that proves each gate is re-run when the gate's model changes.
- **Fixtures and harnesses move with the source.** Fixture rows are states the real flow can produce, with prose composed from the row, never authored beside it; time-anchored fixtures are re-dated as an offset from now by an idempotent script before every rendering session; a harness is regenerated in the commit that changes its source.
- **Live over fixtures.** Click tests and rehearsals run the real path with the real agent; assertions target the live run, never fixture recordings. Fixtures are break-glass for a failed readiness check, nothing else.
- **Deterministic sample data**: static values, no `rand()`, `now()`, or `today()` in seeds; regenerated by script or corrected by targeted in-place writes, never hand-edited.

## 12. Writes against demo data

- **Every automated write names its targets explicitly and never sweeps.** Seeders, backfills, reshifts, and in-place fixes carry an explicit id list — never a query, a range, or "all rows where…" — so the blast radius is readable in the payload before the call is made and a growing data set cannot widen it.
- **Never mint a key as `max()+1` over a row-secured record type**; let the source assign keys, take foreign keys from relationships, or mint from an unsecured type.
- **Cleanup is verified by absence** as a full-scope account, children before parents. Throwaway probe objects carry a `zz` or `tmp` marker in their names and are deleted in the same session that created them, with the deletion confirmed by a 404 or an empty list. Deleting a design object is irreversible over MCP, so a throwaway from another session is deleted on the owner's word, not on inference.

## 13. Project sections (added per build, below this line)

**Build parameters.** The project sections open with this block. It holds the build-specific values that core steps read by name. Copy it to the top of the project sections and fill it in before the first build session.

- **Rule for an unset value.** A value the build cannot have yet — for example an application that Phase 1 has not created — is written `unset` with the reason. A step that needs an unset or blank parameter reports "build parameter `<name>` is unset" and skips the check that needs it. It never guesses a value.
- **Changing a value.** Values change only through an edit to this block. A preflight report never writes one.

```
## Build parameters

| Parameter | Value | Read by |
|---|---|---|
| Application UUID | <uuid of the build's Appian application> | §2 step 2 (design read) |
| App prefix | <object-name prefix, e.g. ABC> | §13 naming; no preflight step reads it |
| Design account | <account the Dev MCP signs in as> | §2 step 8 (identity check) |
| Security groups | <groups the build gates on, with nesting> | §2 step 8 (group readback) |
| Per-session ritual | <script and trigger, or "none"> | §2 step 7 |
| Persona site stub | <URL stub of the site personas use> | §2 step 9 (sail liveness) |
```

Add, in the build's own words: vocabulary canon (exact stored values and display labels); data model and relationships; naming prefix and groups; business rules implemented once as shared rules; demo repeatability rules (session tagging, reset and verify-ready actions, reserved id ranges); known data artifacts that are deliberately not fixed, each with what shows, why, and what to say; the files in the repo and what each is for.

---

# Project sections — Settlement Operations Demo (Appian + Snowflake)

## Build parameters

| Parameter | Value | Read by |
|---|---|---|
| Application UUID | `80384196-bed7-4692-b86b-e7be596fd0bb` (Settlement Operations) | §2 step 2 (design read) |
| App prefix | `SO` | §13 naming; no preflight step reads it |
| Design account | `scott.thorn` (member of `SO Supervisors`, so readbacks are full-scope) | §2 step 8 (identity check) |
| Security groups | `SO Users` > `SO Analysts`, `SO Supervisors` (supervisors also members of analysts); `SO Demo Admins` outside the tree | §2 step 8 (group readback) |
| Per-session ritual | `fixtures/p4-verify-redate.py`, applying all three CSVs, in any session that renders a Phase 4 screen (Repeatability, below) | §2 step 7 |
| Persona site stub | `settlement-ops` (the Demo Admin site `settlement-ops-admin` is not a persona site) | §2 step 9 (sail liveness) |

Reusable joint demo: **Pre-Settlement Fail Prevention** — Snowflake predicts which trades will fail at trade date; Appian turns the prediction into governed remediation before cutoff; resolved outcomes close the learning loop. Everything user-facing must read like it was written by someone who has worked a settlements desk. Positioning, act structure, and phasing live in `pre-settlement-fail-prevention-one-pager.md` and `BUILD_PLAN.md`; this file governs data model, vocabulary, and business rules for the build.

Skill precedence and the promotion route are the operating core's (top of file, and §9); this build has no project skill, so SAIL styling rules for it live under Working style below.

## Vocabulary canon (never deviate)

- Fail reasons (exact 4, from SETTLEMENT_HISTORY): `insufficient_securities`, `funding_gap`, `counterparty_default`, `operational_error`
- Risk tiers (exact 4, from TRADE_PREDICTIONS): Critical, High, Medium, Low
- Remediations (one per fail reason, exact): Arrange cover borrow / partial release; Treasury funding chase; Credit-risk escalation; Correct & resubmit
- Desks (from TRADES.desk): EQ_FLOW, FI_TRADING, FX_DESK, ETF_MM, CREDIT
- Case statuses: New, Agent Triage, Pending Analyst, In Remediation, Resolved - Straight Through, Resolved - Analyst, Escalated
- Dispositions (codes only — prose goes to SO Case Comment): Settled - Borrow Executed, Settled - Partial Release, Settled - Funding Received, Settled - Corrected, Settled - No Action, Escalated - Credit Risk, Failed - Penalty Accrued
- Domain terms: settlement fail, cutoff, T+1, remediation, straight-through, watchlist, notional at risk, fail rate, CSDR penalty. Never "ticket," "SLA breach" as a status, or generic ITSM language.
- **Quantity display language follows asset class**: **"shares"** for equities and ETFs, **"face"** for bonds, **notional only** for FX forwards (a forward has no unit count). Never render a bond quantity as shares.
- **A narrated beat may only point at a trade whose instrument type and fail reason cohere** — no `insufficient_securities` (deliver-side shortfall) story on an FX forward, no borrow remediation on something that cannot be borrowed. Enforced at authoring time in `hero-trade-spec.md`, not discovered on stage.

### Display vocabulary (screen labels only — STORED VALUES ARE UNCHANGED)

The canon above is what is stored and what rules, processes and the agent read. The list below is what an analyst *sees*. Never let a display label reach a stored field, a rule comparison, or the agent's context.

- **Status display labels — ALL SEVEN, ratified 2026-09-03.** Stored statuses are unchanged and remain the seven in the canon; these are what the analyst sees. Rendered solely by `SO_statusDisplay`.
  | stored | displayed |
  |---|---|
  | `New` | New |
  | `Agent Triage` | In triage |
  | `Pending Analyst` | Awaiting review |
  | `In Remediation` | In remediation |
  | `Escalated` | Escalated |
  | `Resolved - Straight Through` | Resolved · straight through |
  | `Resolved - Analyst` | Resolved · analyst |
  The first four upper rows plus Escalated were evidenced by `mockups/analyst_watchlist.html`; the remaining three were proposed (a watchlist shows open work, so no mockup evidenced them) and ratified by Scott.
- **Confidence is governance-screen vocabulary, not analyst-screen vocabulary.** Analyst-facing screens say **"auto-release score N, releases at 80"** — N is the stored 0–1 `confidenceScore` rendered on a 0–100 scale, and 80 is `SO_CONFIDENCE_THRESHOLD` on the same scale. The stored decimal and the constant are unchanged; only the rendering scales. The word "confidence" stays on governance and audit screens, where the gate itself is the subject.
- **Asset class — ratified 2026-09-09.** Stored `equity` / `bond` / `etf` / `fx_forward`; displayed **Equity / Bond / ETF / FX forward**. Rendered solely by `SO_assetClassDisplay`.
- **Counterparty region — ratified 2026-09-09, stored values corrected 2026-09-10.** Stored `NA` / `EMEA` / `APAC` / `LATAM` — **measured on the live counterparty table**; the earlier `EU` / `UK` listing was never stored. Displayed **North America / EMEA / APAC / Latin America**. Rendered solely by `SO_regionDisplay`, which maps all four (its `EU` / `UK` branches are harmless dead code). `NA` is the one that matters: on screen it reads as "not available". APAC stays APAC — it is what a desk says, and expanding it would be less authentic, not more.
- **Probabilities display as percentages** (`p(fail)` rendered `47%`), never as the stored 0–1 decimal.
- **CROSS-CURRENCY HOUSE TOTALS GO THROUGH `SO_fxToUsd` AND RENDER AS "USD eq." — APPROVED 2026-09-09.** The book runs EUR/GBP/JPY/CHF and nothing in the source data carries an FX rate, so a house "value at risk" is not computable from the data alone. The ruling: a **labelled approximation** beats no house view. Indicative static rates live in one rule; every figure derived from them renders with the "USD eq." unit and carries the conversion basis in its tooltip, alongside the Snowflake provenance note. **Phase 5 replaces the rule body with a real rate lookup; the signature is stable, so no screen moves.** Never render an FX-converted figure without the unit and the basis.
- **MATCHED IS STATE-ONLY ON BASELINE DATA, AND THE STAMP IS CONDITIONAL ON THE PACKET — ruled 2026-09-09.** Case Detail renders "Matched Yes / No" and withholds `matchedAt`. `SO Trade.matchedAt` is Snowflake baseline, it is never mutated by a demo run, and it is therefore **stale by construction**: beside a cutoff-derived settlement date it renders a stamp from another year and asserts a contradiction the screen cannot resolve. There is no honest source for it on fixture or baseline rows, so the screen shows the decision-relevant half — an unmatched trade this close to cutoff is a second problem — and says nothing it cannot support. **The gate opens in Phase 5**, where packet trades author `MATCHED_AT` on the **evening of their relative-to-now trade date** (ruled 2026-09-10, the hero-insert precedent; the earlier `cutoff − 4h` formula is retired — `hero-trade-spec.md` §3a, `packet-spec.md` §2); the display condition is "a stamp exists that was authored on this clock", not "the field is populated".
- **A DISPLAY LABEL IS A MAPPING, NEVER A CASING TRICK, and this is the rule the two entries above were bought with.** Case Detail rendered asset class as `upper(left(x,1)) & lower(mid(x,2,n))`, which produced **"Fx_forward"** — a raw enum wearing a capital letter — and would have produced "Etf". Casing cannot know that an underscore is a word break or that ETF is an initialism, because casing is not a vocabulary. Every enum that reaches a screen goes through a named `SO_*Display` rule; if it needs a title-case expression inline, that is the tell it has no mapping yet.
  - **Desks are the exception and are NOT mapped**: `EQ_FLOW`, `FI_TRADING`, `FX_DESK`, `ETF_MM`, `CREDIT` are what the desk calls itself, and the canon lists them in exactly that form.
  - **Audit and event text is analyst-facing and obeys this rule too.** A process-composed event detail reading "Escalated by agent: funding_gap — …" is the same leak in a different container; it is the display label, lower-cased into the sentence, that belongs there.
  - **Standing debt, logged not fixed:** counterparty `riskTier` is still title-cased inline on both the watchlist and Case Detail. It renders CORRECTLY on all four stored values — that is measured, 2026-09-09 — so it is fragile rather than broken, and it is in TODO as `SO_riskTierDisplay`.


## Snowflake environment (ground truth, read before any integration work)

- Account: `a2770166725871-appian-partner.snowflakecomputing.com` (shared Appian partner account; each SC connects with their own credentials). Warehouse: `COMPUTE_WH` (X-Small; each user's default warehouse must be set or record queries fail with "no active warehouse").
- Schema `FINSERV.TRADE_SETTLEMENT`: tables COUNTERPARTIES (50), INSTRUMENTS (200), TRADES (50,000), SETTLEMENT_HISTORY (50,000), TRADE_PREDICTIONS (50,000); semantic view TRADE_SETTLEMENT_ANALYTICS; agent SETTLEMENT_RISK_AGENT; Streamlit app TRADE_SETTLEMENT_DASHBOARD; stage CSV_STAGE.
- Verified baseline numbers (assertion targets): overall fail rate **12.83%** (6,416 / 50,000); tiers Critical 1,824 (avg p 0.794) / High 10,637 / Medium 16,995 / Low 20,544.
- Semantic model ships 5 verified queries — overall fail rate, top failing counterparties, risk tier distribution, fail rate by asset class, notional at risk (High/Critical). These are the Act 2 question vocabulary; do not invent others without testing.
- Scoring path: TRADE_PREDICTIONS is currently populated by the SQL heuristic in run_inference.sql (TRUNCATE-then-INSERT, fully idempotent — rerunning restores pristine predictions). Phase 8 replaces it with TRADE_FAIL_CLASSIFIER!PREDICT_PROBA; the heuristic stays as commented fallback.
- MCP server `TRADE_SETTLEMENT_MCP`, endpoint `https://a2770166725871-appian-partner.snowflakecomputing.com/api/v2/databases/FINSERV/schemas/TRADE_SETTLEMENT/mcp-servers/TRADE_SETTLEMENT_MCP`, PAT bearer auth (token `appian_ny_mcp`, scoped to FINSERVADMIN — expiry in deploy-notes). Tools and input schemas (exact): `trade_settlement_analyst` → input `message` (Cortex Analyst over the semantic view; analytical Q&A); `settlement_risk_agent` → input `text` (full Cortex agent; multi-step reasoning). Analyst is the cheap lookup tool; the agent is the expensive delegation tool — the SO Triage Agent chooses between them.
- **`TRADE_PREDICTIONS.TOP_RISK_FACTORS` exists — a VARIANT array of `{factor, contribution}` — and it holds risk DRIVERS, not fail reasons** (measured 2026-09-10, probe P5): Counterparty Risk (first element on ~48,000 rows), Low Base Risk, Normal Operations, Extended Settlement, High Volatility, Slow Confirmation, Month-End Pressure, Large Notional. **Measured shape:** exactly three elements; `[0]` is Counterparty Risk at **2.5 × the counterparty's historical fail rate** (DOUBLE) on 48,157 rows; `[1]` and `[2]` come from fixed (factor, contribution) pairs per position (`packet-spec.md` §4). **Packet predictions are baseline-shaped (ruled 2026-09-10), and a packet row's factors must never contradict its story's fail reason or lane.** It is **not mapped** on SO Trade Predictions and **not read by intake this phase**. Fail reasons on packet cases are Appian-authored per story through `SO_createTriageCase`'s `failReason` parameter (ruled 2026-09-10). Reading the VARIANT at intake is a later item: it needs a Designer re-sync, and VARIANT mapping on a record type is untested.
- **APPIAN READS SNOWFLAKE `TIMESTAMP_NTZ` AS UTC** (measured 2026-09-10). `TRD000009`'s `MATCHED_AT`, generated as NTZ 19:11, reads back `gmt 19:11` and renders `14:11` for a UTC−4 user. **A "now" written from Snowflake must be the UTC wall clock (`SYSDATE()`), never session-local `CURRENT_TIMESTAMP()` cast to NTZ** — that is off by the session's UTC offset, silently. **The session is NOT UTC:** probe P9 measured the Snowsight session at America/Los_Angeles (`CURRENT_TIMESTAMP` 12:52 −0700 against `SYSDATE` 19:52). **`TRADE_PREDICTIONS.SCORED_AT` carries `DEFAULT CURRENT_TIMESTAMP()`**, so any writer that omits it stamps the Pacific wall clock, which Appian reads as UTC — seven hours old. Packet writes set it explicitly, and packet dates use the UTC date (`SYSDATE()::DATE`), not the Pacific `CURRENT_DATE`.
- Fallback path if MCP misbehaves: `SNOWFLAKE.CORTEX.DATA_AGENT_RUN` is callable as SQL through the data-source connection (structured messages body, not bare strings).

## Appian application (NY environment)

- App **Settlement Operations**, prefix **SO**. Groups: `SO Users` (parent) > `SO Analysts`, `SO Supervisors` (supervisors also members of analysts); `SO Demo Admins` outside the tree. Business groups Viewer; process initiators where they start things; Reset/Verify and Admin page are Demo Admins only. Demo users: `alex.analyst` (SO Analysts), `sam.supervisor` (SO Supervisors). Act 3's Designer tour runs as the presenter's own admin account, never a persona.
- Connected systems: `SO Snowflake Data Source` (native 26.6 Snowflake Data Source template — the plain "Snowflake" tile is the AppMarket plugin and is never used); `SO Snowflake MCP` (generic MCP connected system, bearer PAT).
- Record types on Snowflake via **direct data access, never sync** (the no-copy story is the demo's argument): `SO Trade` (TRADES), `SO Trade Prediction` (TRADE_PREDICTIONS, 1:1 on trade_id), `SO Counterparty` (COUNTERPARTIES, N:1 from Trade — Phase 1). Record-level security: analysts scoped by `desk`, supervisors see all — defined on SO Trade only, inherited per appian-supplemental §12. **`alex.analyst` is desk EQ_FLOW; the SO Analysts security rule is hardcoded to that desk (production would map user→desk).** Consequence for readbacks: any row count taken as an analyst is EQ_FLOW-scoped, never a total (appian-supplemental §5).
- Appian-native record types (cloud DB): `SO Settlement Case` — caseId, tradeId, sessionId, status, failReason, proposedRemediation, confidenceScore, **disposition (Text, codes only)**, **assignee (User type — powers the `assigneeUser` relationship and the Assign action's picker)**, desk, cutoffTs, created/resolved timestamps; `SO Case Comment` (createdOn is a plain DATETIME stamped explicitly by the Add Comment write path — not auto-managed; deliberate, matches the events pattern). Every demo-created row carries sessionId. Audit is native record events on SO Settlement Case (Event History + Event Type generated; RELATED_RECORDS security added by hand; 7 seeded event types; app-wide commenting disabled so the comment field carries process-composed audit detail. **Comment column width MEASURED 2026-08-28 through the production Write Records node: 4000 characters** — a 4000-char write stored 4000 with no truncation, so the `getRecordType` readback of VARCHAR(255) UNDER-reports the real column).
- The Dev MCP design account is a member of SO Supervisors — its readbacks are full-scope.
- `SO Settlement Case` N:1 `SO Trade` is deliberate — trades are permanent baseline, cases are session-tagged and disposable, so the same trade accrues cases across demo runs and concurrent SC sessions. Operational one-case-per-trade holds only WITHIN a session, enforced by the Phase 3 create-case path (check for an existing open case in the session before writing), never by the relationship.
- Data-fabric purity per appian-supplemental §12: no CDTs, no data store entities, anywhere.

## MCP servers and executing identity (READ BEFORE ANY TOOL CALL)

**This section applies §6 to this build.** Two Appian MCP servers are configured. As of 2026-09-21 they are registered by role — the Dev MCP as `appian` (tools under `mcp__appian__`) and the runtime server as `appian-runtime` (tools under `mcp__appian-runtime__`), observed in that session's tool list. Until then both surfaced under the one `mcp__appian__` namespace — the merge case §6 describes — so build-log entries before that date read the tool-family tell, not the prefix. The tell still holds either way.

| | Dev MCP | Runtime MCP |
|---|---|---|
| Server | `lcp-mcp-server` (local, `uv run`) | Appian runtime MCP |
| Executes as | **`scott.thorn`** — full design + `SO Supervisors` scope | **`scott.mcp` Service Account** — created for and scoped to the **Starwood** demo, a different application. **No scope in this app.** |
| Tool naming | camelCase, no prefix — `listRecordTypes`, `getRecordType`, `testRule`, `testProcessModel`, `listRecordData`, `createExpressionRule`, `updateProcessModelNode` | snake_case with an `appian_` prefix — `appian_invoke_process_model`, `appian_data_fabric_sql_query`, `appian_search_tools`, `appian_get_tool_schema`, `appian_invoke_agent`, `appian_get_process_status`, `appian_data_fabric_metadata`, plus `ping` |
| Use here | **Everything** | **BANNED — no exceptions** |

**BINDING RULE: every `appian_`-prefixed tool and `ping` are BANNED for this project.** Not for tests, not for break-tests, not "just to check". They execute as `scott.mcp`, a foreign-scoped service account. Processes run through **`testProcessModel`**; reads through **`testRule`** or wrapper interfaces per the supplemental. `scott.mcp` gets **no group membership and no scope in this application, ever** — it stays Starwood-only.

**Why this matters more than it looks (measured 2026-09-02/03 — the incident behind §6's "why").** Row-secured reads under a foreign identity return **empty, silently**, while writes targeted by primary key still **succeed** (supplemental §5). A triage run started through the runtime server therefore completes green, having read nothing: the agent received an all-`not available` context, fabricated a confident ERROR assessment, and the process wrote status `Escalated`, a case comment and a type-7 audit event — **a forged escalation, with every error variable reporting success.** The tell that isolated it: on the same runs the Case Created event, composed from the create process's own parameters rather than a record read, was **correct**. Reads were blind; parameter-composed text was fine.

## PREFLIGHT — project applications of §2

The operating core's §2 runs first, in full (plan gate, design-surface check and STOP, version and sail pins, server/identity mapping, skill-copy comparison). This build adds:

1. **Identity (§2.8):** the Dev MCP runs as `scott.thorn`, member of `SO Supervisors` — every readback is full-scope unless stated otherwise; the runtime server is banned (table above).
2. **Per-session ritual (§2.7):** any session that renders a Phase 4 screen re-dates the P4-VERIFY fixtures first (Repeatability, below).
3. **There is no Snowflake execution path from a session.** All Snowflake DDL/DML is delivered as a script for Scott to run in Snowsight, written to `snowflake/`. Never attempt to connect to Snowflake directly. Snowflake *data* is readable through the Appian record layer (`listRecordData`, `testRule` over the record types) — that is a read path, not an execution path.

## Snowsight deliverables (BINDING, every phase — ruled 2026-09-11)

The generic rule is `reference/toolchain.md` §11; this is its application here, with the history that bought it.

- **Anything Scott is asked to run in Snowsight delivers its evidence as ONE final statement returning ONE grid**, with columns `check_name | result | detail` and `result` printing `PASS` or `FAIL` per check.
- **Multi-statement verification blocks whose results must be clicked through or exported are PROHIBITED.** Snowsight displays and exports one grid — whichever is showing. Ignoring that cost three round-trips on 2026-09-10/11:
  - a probe whose CSV export carried only its last result set;
  - a retrofit (P9) folding the rest into one grid;
  - an eleven-grid verification block that could only be read by matching comment labels to SQL.
- **Where evidence needs intermediate states** (a load, a reload, a reset), the working form is **one Snowflake Scripting block**:
  - `EXECUTE IMMEDIATE $$ … $$` performs the steps and records each check into a session-temporary table.
  - It ends with `RETURN TABLE(...)` over that table.
  - An `EXCEPTION` handler appends a FAIL row carrying the error text, so a stopped run still returns the checks that ran.
  - Every object is fully qualified, because the statement relies on the worksheet's role and warehouse, not on `USE`.
  - First instance: `snowflake/verification-summary.sql`.
- **Setup is not evidence.** A script that creates objects may run many statements; its verification is a separate one-grid statement.
- **If a deliverable genuinely cannot be one statement,** each grid-producing statement is explicitly marked `-- SCREENSHOT THIS GRID`, and their number is kept to a minimum.

## Business rules

- Reason → remediation mapping is ONE rule (`SO_remediationForReason`): the four pairs from the vocabulary canon, each with an SLA urgency. The SO Triage Agent's propose-remediation tool wraps this rule; the agent never invents remediations.
  - Verified live 2026-09-02: `insufficient_securities` returns `Arrange cover borrow / partial release` / `HIGH` / `Settled - Borrow Executed`. All four pairs and the `Manual review` fallback break-tested through `testRule`; canon and build agree.
- Priority = risk × time-to-cutoff (`SO_casePriority`), computed from FAIL_PROBABILITY and EXPECTED_SETTLEMENT_DATE. A medium-risk trade settling in 90 minutes outranks a high-risk trade settling Thursday. Drives analyst queue sort.
- Confidence gate: XOR on `confidenceScore` against constant `SO_CONFIDENCE_THRESHOLD` (demo-tunable live; changing it visibly changes the straight-through rate). Above: straight-through resolution with audit write + notification. Below: analyst task with the agent's full reasoning attached. Separate branch for clarification/escalation.
- **ESCALATION FOR A BREACHED WINDOW IS A PROCESS-LAYER POLICY, NOT AN AGENT JUDGMENT — ruled 2026-09-22.** If broker confirmation lag exceeds the hours remaining to cutoff at triage time, `SO_triageCase` escalates, **ahead of both the straight-through gate and the analyst lane**: once the window is gone, releasing and referring are both wrong. The `counterparty_default` reason override is unchanged and still escalates on reason alone. **Why it is not left to the agent:** the same case at the same score routed two different ways on two runs, because the agent's `escalate` flag is not deterministic, and a narrated beat cannot depend on a coin-flip. The score is still recorded, shown and audited — it just does not decide this lane. The breach arithmetic (lag, hours remaining, score) is composed into the escalation event from values in hand, per the fixture-prose canon, and every escalate path renders the fail reason through `SO_failReasonDisplay`, lower-cased into the sentence.
- confidenceScore semantics: the agent's confidence that the proposed remediation is the correct course of action, based on case facts. **The model's fail probability is explicitly excluded** — probability answers "will this trade fail" and is why the case exists; confidence answers "is this the right fix". Never a guarantee of execution feasibility (lending inventory, treasury balances), which is downstream desk work. **Only genuinely concerning facts lower it** — unusually large notional for the desk, a high-risk counterparty or above-average historical fail rate, a tight window; concerning facts compound. **Favorable facts are reassurance and must not lower it**, and a case favorable across the board earns 0.85 or higher. The timing arithmetic is mandatory: broker confirmation lag against hours remaining to cutoff, both numbers stated; lag at or near the remaining hours means the remediation cannot complete and the case goes to a human at confidence 0.5 or lower.
- **`SO_caseContext` shape (the agent's entire view of a case).** Six labeled lines, in this order, assembled process-side in one query — never four agent tool calls (§11: Agent Studio record-data tools do not traverse relationships):
  1. `CASE:` id, status, desk, fail reason, cutoff + **hours from now** (the denominator of the timing arithmetic)
  2. `TRADE:` trade id, instrument id, direction, **notional thousands-separated** (raw concatenation emitted `1.87005e+07` to the agent — always format), expected settlement, desk, broker confirmation lag (the numerator)
  3. `INSTRUMENT:` ticker — name | instrument id, so the agent says "SAP GY / SAP SE" rather than reciting `INS0147`
  4. `PREDICTION:` fail probability, risk tier, scored-at, predicted reason
  5. `COUNTERPARTY:` name, type, region, risk tier, historical fail rate
  6. `ONWARD DELIVERIES:` a plain statement, not a number to re-derive — either "no onward deliveries … contained to this settlement" or "N onward deliveries … a fail may cascade", from `SO_onwardDeliveries`
  **Penalty accrual is deliberately NOT in the context.** `SO_penaltyAccrual` is a screen figure — what another day costs — not a fact the agent should weigh when judging whether a remediation is the right fix. Keeping it out preserves the confidence semantics.
  **Pending a Designer re-sync**: ISIN, settlement venue (CSD), and matched status + matched time. The Snowflake columns exist; the record-type fields do not, and `addRecordTypeField` cannot map an existing external column (measured — see below).
- **Mapping an already-existing external column onto a record type is a DESIGNER RE-SYNC, not a Dev MCP operation.** `addRecordTypeField` with `updateTable: false` returns `HTTP 400 — Non-CDM field operations are not yet supported through this API`; with `updateTable: true` it would ALTER the source table, which for these Snowflake tables is forbidden (all Snowflake DDL goes through a Snowsight script). So new Snowflake columns land in Appian only when Scott re-syncs the record type in Designer.
- `SO_CONFIDENCE_THRESHOLD` stays 0.80 — bank-realistic straight-through bar, deliberately not tuned to the agent.
- Straight-through audit trail must render what the agent saw, which tools it called, and why it cleared the gate — the audit is the governance story, not a log.
- **Case-creation criterion — `RISK_TIER` is `High` or `Critical`, ruled 2026-09-10.** Source: Scott's ruling, aligned with the semantic model's verified query *notional at risk (High/Critical)* — one risk language across case creation, the screens and Cortex. **Implemented ONCE, as a tier check in the Phase 5 intake model**, reading the mapped `riskTier` field — built 2026-09-11 as `SO_intakePlan`, read by `SO_intakeRun`. Measured bands on the baseline: High p 0.5003–0.7495, Critical 0.7507–0.95. Before this ruling no object implemented a criterion (measured: `SO_createTriageCase` has no caller and gates only on duplicates; Phase 3 specimens were hand-picked). A probability-floor constant is a **talk-track roadmap line, not built**. Contract: `packet-spec.md` §3.
- Disposition is a CODE, never free text — it records how the risk cleared (the month-end analytics dimension), distinct from status (where the case sits in the workflow). Analyst prose goes to SO Case Comment. Settled - No Action is the false-positive counter feeding the Phase 8 learning loop-back.

## Repeatability (hard requirement — multi-SC asset)

- **RE-DATE THE P4-VERIFY FIXTURES AT THE START OF EVERY SESSION THAT RENDERS A PHASE 4 SCREEN.** Run `fixtures/p4-verify-redate.py` and apply **all three** of its CSVs through `updateRecordData` — `p4-verify-cases.csv` to SO Settlement Case, `p4-verify-comments.csv` to SO Case Comment, `p4-verify-events.csv` to SO Settlement Case Event History — before rendering anything. **THREE TABLES MOVE TOGETHER OR THE AUDIT TRAIL LIES:** the assessments and the activity timeline are stamped on the same clock as the cutoffs, and re-dating only the cases leaves the agent apparently running days after the case opened. Until 2026-09-09 the script's docstring claimed three CSVs while its `main()` emitted one, so the other two were being done by hand against a script that looked complete — the harness-goes-stale failure in miniature. Fixture cutoffs are absolute timestamps and decay daily: a session that renders without re-dating shows a book entirely past cutoff and an "Inside critical window" KPI of zero, which reads as a broken screen rather than stale data. The script is idempotent — every case is pinned to an OFFSET FROM NOW, never shifted by a delta — so running it twice is harmless. It re-dates triage stamps on the same clock, because the stored assessments quote "roughly N hours remaining to cutoff" as of triage; letting the two drift apart rebuilds the fiction defect automatically.
  - **THE HEADROOM RULE, learned by rendering rather than by reasoning:** the rail recomputes "does the remediation fit the window" from the LIVE clock while the assessment states the arithmetic as of triage. A case whose assessment says the remediation FITS needs `cutoff offset >= broker confirmation lag + ~3h`, or the two agree at re-date time and silently diverge within the hour — rail saying "does not fit" beside an assessment saying it fits. A case whose assessment says it does NOT fit is safe at any offset, because time only makes that verdict more true.
  - **Contrast, deliberately: the Phase 5 packet inserts cutoffs RELATIVE TO NOW at insert time, so a simulated feed never needs this.** This ritual exists only for the hand-authored verification fixtures, and it disappears when they do.
- **FIXTURE ROWS MUST BE STATES THE REAL FLOW CAN PRODUCE, AND FIXTURE PROSE IS COMPOSED FROM THE ROW, NEVER AUTHORED BESIDE IT.** Status, escalate lane, confidence, remediation and assessment presence all have to agree, and reason overrides bind — `counterparty_default` is never released, whatever it scored. A scored case has an assessment; a `New` case has neither score nor assessment. Every figure in the prose is read off the record, using the SAME rounding the screen uses (`round()`, not `text(x,"0")`, which truncates — a truncating dump had assessments saying 82% where the rail rendered 83%). A fixture whose narrative and data disagree is worse than no fixture: the screen looks alive while it lies, which is the forged-escalation failure mode with a human in the fabricating seat.
  - **A TIMESTAMPED ASSESSMENT DISAGREEING WITH THE LIVE CLOCK IS CORRECT, AND NO SESSION MAY "FIX" IT — ratified 2026-09-09.** On the past-cutoff case the agent card is stamped 06:00 and says "1.0h remaining" while the TIMING card beside it says "Past cutoff". That is not an inconsistency; it is an **audit artefact next to a live reading**, and the contrast is a governance talk-track beat: the machine's reasoning is a dated record of what it knew when it ran, and the clock kept going. Making the two agree would require either back-dating the clock or rewriting history — the second of which is exactly what an audit trail exists to prevent. Leave it. The same holds for the headroom drift below: an assessment saying "roughly 4.0 hours remaining" beside a banner reading "2h 42m" is the same artefact, correctly stamped.
  - **AND THE PROSE IS COMPOSED FROM THE ROW AS IT STOOD AT THE RUN TIMESTAMP, NOT AS IT STANDS NOW** (2026-09-09). An assessment is a dated artefact: it says what was true when the agent ran, and the screen stamps it with that time, so its arithmetic must reconcile against the run rather than against the clock. The past-cutoff fixture had been written from the *demo's* vantage — "the cutoff has now passed" — while its own run stamp sat an hour BEFORE that cutoff, and the event log written from the same moment correctly said "1.0h remaining". Two artefacts of the same instant contradicting each other, both rendered faithfully. Compose from the row at `runAt`; let the live cards say what is true now. The re-date script's `triage_offset()` guarantees the run precedes the cutoff, which is what makes this checkable at all.

- **A PROCESS-WRITTEN CASE HAS NO `createdOn` AND NO `modifiedOn`, AND A FIXTURE CASE HAS BOTH — MEASURED 2026-09-23.** All three cases written by `SO_intakeRun` / `SO_triageCase` read back NULL on both fields; all three fixture resolved cases read back populated, because `p4-verify-redate.py` writes them. **Two binding consequences.** (1) **Any screen that formats either field must guard the null**, or it takes the whole page down: `SO_supervisorCommand` line 351 calls `text(modifiedOn, …)` unguarded where line 345 guards the same field, and since every demo run produces exactly one straight-through resolution, the supervisor screen fails for the entire demo window. (2) **A process-created row's "when" is read from a field the process actually wrote** — `resolvedOn` for a resolution, or the packet prediction's `SCORED_AT` for the load time, which is what the Demo Admin console's status line uses. Never reach for an auto-managed timestamp on a row a process created. The general form — *a fixture set and a process-written set can differ in which system-managed fields are populated, so a screen verified against fixtures is not verified against production rows* — is staged for promotion, not yet portable.
- Every demo run is session-tagged (sessionId on cases, comments, audit, resolution events). Snowflake baseline data (the 50K) is never mutated by a demo run; demo-created Snowflake rows (Phase 5+ simulation, Phase 8 loop-back) carry sessionId too.
- `Reset Demo` (Demo Admins): deletes session-tagged Appian rows, cancels orphaned processes, runs the Snowflake session-row cleanup. `Verify Ready` (Demo Admins): baseline counts, zero open demo cases, MCP reachable (one cheap tool call), warehouse warm — green-checks screen.
- Feed simulation: fixed scripted packet — **15 trades: 3 seeded High/Critical stories that open cases, 12 background trades below the High floor (p < 0.5003) that do not**. Contract `packet-spec.md`; ids `TRD9 || <run name without dashes, ≤13> || 01..15`, reset by exact id list. Identify seeded specimens by properties, never platform-assigned ids (appian-supplemental §5).

## Known data artifacts (DO NOT FIX — explain them if asked)

Facts about the generated baseline that look like defects on screen and are not.
Each has been checked, each is left alone deliberately, and each has a one-line
answer for the room. Anything added here needs the same three parts: what shows,
why it is there, what to say.

- **RECENT-FAILS DATES LAND ON WEEKENDS.** `SETTLEMENT_HISTORY.ACTUAL_SETTLEMENT_DATE` was generated without a settlement calendar, so Saturdays and Sundays appear — **measured on the two gate B screens themselves: 5 of the 10 rendered rows across cases 36 and 39 fall on a weekend** (`Sat 5 Jul`, `Sun 6 Jul` on both, plus `Sun 13 Jul`). No real settlement fails on a Saturday. **No data change**: correcting it means rewriting rows in the 50K baseline, which the repeatability rule forbids, and the dates carry no argument — the card's argument is the *reason* mix, which is real. **Talk track: "synthetic history."** Say it once if anyone notices; do not pre-empt it.
- **SAM SUPERVISOR PERSONALLY HOLDS A CASE** in the Analyst Queues card (SO-45, FX_DESK). Fixture artifact — the assignee was set when the fixtures were authored, before the narrative settled. In the demo's own story supervisors **direct** work rather than hold it. **No data change**: it is one row, it is not wrong (a supervisor may take a case), and the card is honest about who holds what. **Talk track: "reassignment demo fodder"** — it is a ready-made reason to open the Assign action from the supervisor's own screen.
- **The newest settled fail is ~13 months old** (history spans 2023-01 → 2025-07 against a 2026 system date), which is why the recent-fails card claims no time window at all rather than the mockup's "last 30 days". Same reason: the baseline is not re-dated.

## Working style

- **TOP-LEVEL LAYOUT FOR EVERY FULL-PAGE SCREEN IS `a!formLayout(contentsWidth: "FULL")`, never `a!headerContentLayout`.** MEASURED 2026-09-03: `a!headerContentLayout` has NO width parameter at all — its full signature is `(header, contents, showWhen, backgroundColor, contentsPadding, isHeaderFixed)` — so an interface built on it cannot control its own width and renders as a centred fixed-width band. `a!formLayout` is the only top-level layout with an explicit contents-width control; `contentsWidth: "FULL"` renders as `formWidth "FULL"`. Set `focusOnFirstInput: false` so the page does not steal the cursor into a filter on load; omit `titleBar` and `buttons` so no form chrome appears. **Trap:** `validateExpression` ACCEPTS `contentsWidth` on `a!headerContentLayout` with no errors — only the object validator rejects it. `validateExpression` is not a reliable keyword check.
- **A KPI BAND'S SUB-LINE MUST FIT ONE LINE, OR THE BAND GOES RAGGED.** Six tiles across a full-width form are equal height only while every sub is a single line; one caller writing a long sub silently makes its own card taller and the row uneven. Budget at 1440px is roughly **37 characters** for the narrowest card in a six-card band (one sixth of ~1400px, less card padding, at SMALL rich text) — an ESTIMATE, not a measurement, so treat it as a design ceiling and confirm in a browser. Qualifications that do not fit are not deleted: they go in `SO_kpiCard`'s `tooltip`, which composes them with the Snowflake provenance note so a marked card never loses its explanation. (Learned when a 65-character sub carrying a sampling note made card 1 taller than the other five.)
- **A FIXED-GLYPH BAR MUST OWN ITS LINE. NEVER PUT ONE IN A FLEXIBLE SLOT BETWEEN FIXED SIBLINGS.** The supervisor's reason bars were built as a `a!sideBySideLayout` of `3X` label / flexible glyphs / `3X` figures: the fixed siblings claimed their width first, the glyph run got the remainder of a MEDIUM_PLUS rail, and `preventWrapping` turned the overflow into a bar chart ending in an ellipsis. Same starvation family as the gate-A ticker beside a `MINIMIZE` sibling. **The working form is the label on its own line and the glyph run plus its figures on the next, in ONE rich text field with `char(10)` and NO `preventWrapping`** (which would collapse those paragraphs anyway). **Size the budget from a bar already proven in the same container**, not from arithmetic: the Analyst Queues card renders 14 glyphs with a 41-character trailer in that rail without truncating, so 14 glyphs with a 15-character trailer is safe by construction.
- **CARD HEIGHT: `a!cardLayout(height:)` EXISTS AND IS THE WRONG TOOL FOR ALIGNMENT.** Valid values EXTRA_SHORT / SHORT / SHORT_PLUS / MEDIUM / MEDIUM_PLUS / TALL / TALL_PLUS / EXTRA_TALL / AUTO (default). **A fixed value makes the card's contents SCROLL when they do not fit** — that is clipping with a scrollbar, and a supervisor scrolling inside a card to reach the fourth analyst queue is worse than an uneven column edge. There is **no "stretch to fill"** and **no cross-column bottom-align**: `alignVertical` positions content within a row, it does not stretch a card, and `a!cardGroupLayout(cardHeight:)` equalises cards with *each other* while inheriting the same scroll behaviour. **Balance two columns by shortening the elastic content (a chart's `height`) and by spacing, never by fixing card heights.** The residual gap is a browser measurement — chart and card heights are named enums with no documented pixel values and the render tree carries no geometry.
- **RAIL/PANEL TYPE: ONE SCALE, TWO EXCEPTIONS.** `SMALL` muted labels; `STANDARD` **semibold ink** values — every value: notional, score, lag, counterparty, settles, side. Two exceptions at `MEDIUM`: **the ticker**, and **the probability value plus the live countdown value** (in semantic colour), both short by construction so neither can wrap or truncate. Their context — the tier word, "to cutoff · 15:00" — stays STANDARD or SMALL. **Nothing else exceeds STANDARD. There is no LARGE anywhere.**
  - **THE MOCKUP CSS IS THE STRUCTURAL TYPE AUTHORITY — map to the nearest platform size, never invent a scale.** `mockups/analyst_watchlist.html` puts the whole rail inside a FOUR PIXEL range (labels 11–11.5px, values and prose 12.5–13.5px, one 15px element: `.pv-head .instid .tick`). The build's deliberate correction is *weight and colour* — the mockup's values are a thin mid-grey, ours are ink and STRONG — not size.
  - **EMPHASIS IS WEIGHT AND COLOUR, NEVER SIZE. If a render reads flat, the escalation path is weight and spacing, never a third size.**
  - **Why the rule is this blunt (measured twice):** a "key number" tier at LARGE made the rail worse three ways at once — a giant countdown chip that read as a headline rather than a status, a notional truncated to `38....` by the `preventWrapping` large type needed to survive its column, and sizes jumping line to line so nothing had a calm baseline. Before that, MEDIUM_PLUS on a ticker in a `MINIMIZE`-squeezed side-by-side rendered it **one character per line**. Large type in a 320px rail has almost no horizontal room.
  - **Prose:** one sentence per panel (the verdict), and within it only the verdict phrase is bold + coloured ("Fits the window —"), the rest plain ink — the mockup's own `.verdict`/`.verdict b` pattern. Everything else is a label-value pair.
  - **There is no `STANDARD_PLUS`.** The enum is exactly `SMALL, STANDARD, MEDIUM, MEDIUM_PLUS, LARGE, LARGE_PLUS, EXTRA_LARGE`; the rung above STANDARD is MEDIUM. The validator names the legal set in its error.
  - **A NARROW PANEL'S IDENTITY LINE OWNS ITS WIDTH.** Where a fixed-width item (`width: "MINIMIZE"`) sits beside identity text, MINIMIZE claims what it needs FIRST and the text truncates. `shouldWrap` would reflow but is rejected by the object validator, so the branch is made **in the expression** on the one thing that drives it — text length. In the rail: a ticker of 10 characters or fewer shares line 1 with the countdown chip; anything longer takes the line alone and the chip drops beneath. **The full identity string always wins the line.**
  - **ANY AGENT-AUTHORED CONTENT CARRIES AI ATTRIBUTION, AND IT REPLACES THE LABEL RATHER THAN JOINING IT.** A chip (`AI`, `#E7F0F6` on `#3E7396`) + the agent's name + the run time, SMALL muted — one line that is both label and disclosure. A screen that renders machine-written reasoning without saying so is making an implicit claim about its provenance; this is governance, not decoration, and it applies on every screen that shows agent output.
  - **ONE DOOR.** Where a panel excerpts a record it links to, there is exactly ONE link to that record — the primary navigation at the panel's foot. An excerpt ends with a plain unlinked `" …"` continuation cue. Two links to the same place is a decision the reader should not have to make.
  - **AN EXCERPT SKIPS WHAT ITS CONTAINER ALREADY SAID, AND DETECTS THE SKIP BY CONTENT.** Stored assessments open by restating trade id, instrument and notional — correctly, because the assessment is a standalone audit record — but a panel whose header just said all three would spend its excerpt on nothing. Start at the first sentence carrying REASONING, matched on the vocabulary of the reasoning itself (`broker confirmation lag`, `risk tier`, `twelve months`, `fail rate`, …) via `SO_textContainsAny`, never on a fixed position: "skip sentence 1" is a rule about today's authored shape and will drift. No match falls back to sentence 1, so an unfamiliar shape degrades to "show the beginning" rather than to blank. **Agent instructions are never edited to suit a screen — this is display-side selection over a record that stays complete.**
  - **A PREVIEW PANEL PREVIEWS; THE RECORD IS THE RECORD.** Stored prose is cut at the **second sentence boundary** (split on `". "` — safe because every figure in this domain has its period followed by a digit or `%`) and followed by a link to the record. Identifiers that exist only for plumbing — the case id — and identifiers already visible in the adjacent context — the trade id on the selected grid row — are **omitted from the preview** and kept on the detail screen.
  - **SAIL rich text has ONE bold** (`style: "STRONG"`), so the mockup's 650-vs-700 weight distinction cannot be reproduced; both land on STRONG. Mockup delta, not worked around.
- **GRID ROW SELECTION USES `SUBTLE_HIGHLIGHT`, NOT `ROW_HIGHLIGHT`, ON ANY GRID WHOSE ROWS CARRY SEMANTIC COLOUR.** The four styles are `CHECKBOX`, `CHECKBOX_SUBTLE_HIGHLIGHT`, `SUBTLE_HIGHLIGHT`, `ROW_HIGHLIGHT`; `ROW_HIGHLIGHT` paints a SOLID blue fill and `SUBTLE_HIGHLIGHT` a light tint. Under the solid fill the watchlist's red past-cutoff countdown, amber priority band, grey Unassigned owner and all seven status colours became unreadable at once — and those colours are how the row carries meaning. **The tint hex is platform-controlled and not author-settable; the choice between solid and subtle is the only lever, and it is a COMPONENT parameter — no site accent, theme object or branding setting is involved.** (`updateSite` exposes only name, displayName, description, webAddressIdentifier, layout, style and pages — there is no accent colour on the MCP surface at all.) Rendered by `SO_railFact` and by nothing else, so the hierarchy cannot drift per card; `tier: "KEY"` promotes a fact. Prose budget on a panel is ONE sentence — everything else is a label-value pair. Increase spacing rather than shrinking text; a panel may scroll, and cramped-beats-unreadable is false. Rich text sizes, enumerated by the platform's own validator: `SMALL, STANDARD, MEDIUM, MEDIUM_PLUS, LARGE, LARGE_PLUS, EXTRA_LARGE`.
- **RECORD ACTIONS LIVE IN THE RECORD HEADER, NOT IN A CARD ON THE VIEW — ratified 2026-09-09.** Related actions are configured as `relatedActionShortcuts` on the record VIEW and render beside the view's tab, which is where an Appian user already looks for them; the Related Actions tab is then hidden (`hideRelatedActionsView: true`), because a second copy one tab away is only a second place to look. A view builds its own `a!recordActionField` only where an action belongs to a *region* of the screen rather than to the record.
  - **MEASURED: shortcut ORDER is not author-controlled.** `relatedActionShortcuts` was sent as Record Disposition / Assign / Escalate / Add Comment and read back as Assign / Add Comment / Record Disposition / Escalate — the record type's own action order. To change what the header shows first, reorder the actions on the record type in Designer; the view parameter will not do it.
  - **THE COST IS REAL BUT SMALLER THAN IT LOOKED — CORRECTED 2026-09-24.** The action visibility expressions become the ONLY gate, and `testInterface` cannot see the result: the header renders outside the view interface, so a harness render shows no action set at all, and a visibility expression that throws hides its action and reports nothing (supplemental §4). **What is NOT true any more is that this makes the status→action mapping browser-only. sail reads the record header and drives it.** MEASURED in the Part C rehearsal: as `alex.analyst`, the hero case's header listed `Record Disposition · Assign · Escalate · Add Comment` — in the record type's own order — and `Record Disposition` was followed, filled and submitted through to a written disposition, verified by a fresh read. **So the mapping is verified as a persona through sail, per case status, like any other content-and-behaviour check; only the geometry of the header stays a browser check.**
- **A SCREEN RENDERS THE STORED DECISION; IT NEVER RE-DERIVES ONE.** The lane an analyst sees comes from the case's `status`, never from re-comparing `confidenceScore` to the gate. The two can legitimately differ — a reason override escalates a high-scoring case — and a re-derived lane announced "Released without touch · score 95" beside an Escalated chip (MEASURED on a click-through, case SO-43). Showing the gate arithmetic is fine and is the governance story; claiming an outcome the record does not carry is not. Where they diverge, say so on the screen rather than leaving it looking self-contradictory.
- **A DERIVED-STATE DISPLAY RULE CARRIES ITS STATE TO CONSUMERS RATHER THAN LETTING THEM RE-DERIVE IT** — `SO_cutoffDisplay` emits `isPast`/`hasCutoff`, `SO_autoReleaseDisplay` emits `lane`. Consumers read; they do not recompute.
- **Grids: set an explicit width on EVERY column and `preventWrapping: true` on every single-line cell.** Relative (`1X`) widths divide the available space equally and wrap headers and values mid-word ("Priorit y", "Cutof f in"). One column — the widest, normally Instrument — takes `AUTO` and absorbs the remainder. NOTE `truncateText` appears in the rendered grid tree but is NOT a settable parameter; a rendered-tree attribute is not proof of a writable one.
- Mockup-first: every Phase 4 interface gets an HTML mockup in `mockups/` before build. Mockups are aspirational; build to "as close as possible in SAIL" per appian-supplemental's platform deltas — information hierarchy over pixel styling. Prompts are developed collaboratively in-session, not fired as mega-prompts.
- **A STRUCTURAL DELTA IS A DELTA AND GETS LOGGED LIKE A DATA ONE** — the rule is §7's ("when a built layout departs from its mockup, say so in the log and fix the mockup in the next mockup pass"). Why it bit here: close-outs recorded every figure that could not be built — the runway's missing "on track" band, the absent day-over-day comparisons — while treating *layout* departures as too obvious to write down, so a rearranged card order was invisible in a diff of numbers and the next session would "restore" the mockup's shape. (Learned at gate C: the supervisor layout was reorganised by ruling into a wide-left/rail-right zone, where the mockup's own DOM puts the trend chart beside the runway and the reason/queue cards inside the left column.)
- Site branding (header, accent) is site-level configuration; "Appian + Snowflake" badge in the site header. UI is persona-authentic. Snowflake leverage is made visually legible via a consistent provenance marker — tasteful and systematic, never vendor-logo plastering. The talk track introduces the marker once in Act 1.
- **THE PROVENANCE MARKER IS RENDERED BY ONE OBJECT — `SO_snowflakeMark` — AND BY NOTHING ELSE.** No screen may build the mark inline. It takes a single `showWhen` input, and a bare `rule!SO_snowflakeMark()` fails — always pass it.
  - **CORRECTION, MEASURED 2026-09-09: the general claim that "interface rules require every declared parameter to be passed" is NARROWER than it was written here.** A `tooltip` input was added to `SO_kpiCard`, whose four existing calls in `SO_analystWatchlist` pass the other seven and not the new one; the watchlist re-rendered with `diagnostics.error: null` and correct output. So an omitted input arrives null rather than erroring, and **an optional input can be added to a shared interface rule without touching its callers.** What was actually observed with `SO_snowflakeMark` was a zero-argument call, which is a different case and is left standing as written. Do not use this as licence to omit arguments casually — a null `showWhen` still hides a mark silently — but do not rewrite every caller out of fear either.
- **`SO_snowflakeMark` PREVIEWS BROKEN IN DESIGNER, AND THAT IS CORRECT — DO NOT "FIX" IT.** Opening the object shows *"Rich text icon must be contained (directly or indirectly) within a rich text display component"*. It is a **fragment rule**: it returns a bare `a!richTextImage` / `a!richTextIcon`, which is not a component and cannot stand alone — it is legal only where its callers put it, inside an `a!richTextDisplayField`. The preview has no call site, so it has no container, so it complains. **Preview errors standalone by design — fragment rule; render via any call site.** Verify it through a screen that uses it, never through its own preview. Wrapping the return in `a!richTextDisplayField` to silence the preview would make the rule a component and force every caller to nest a display field inside a display field, instead of placing a mark inline beside its label. The rule's own header comment says all of this; the note is here so a session that never opens the file still knows.
- The intended asset is the **in-system Snowflake logo image document** — not a character, not an icon-font glyph. Document **"Snowflake Logo"**, id `_a-0000eebe-43e3-8000-9c26-011c48011c48_424351` (PNG, 3,519 bytes), in the **Financial Services Data Fabric Examples** application, folder `1dd4400e-e259-4712-8ae1-415e8959feed`, referenced through constant **`SO_SNOWFLAKE_LOGO`** (`_a-0001f054-…_562336`). **`a!richTextImage` takes only `(image, showWhen)`** — icon-sized by definition, no size/link/tooltip parameter, so the tooltip rides on the enclosing `a!richTextDisplayField(tooltip:)` and hover text on `a!documentImage(caption:)`. The U+2744 `❄` character marker is SUPERSEDED and must not reappear.
- **THE LOGO IS LIVE — `SO_MARK_USE_LOGO` is `true` as of 2026-09-09 — AND THE FALLBACK STAYS.** Viewer rights were granted **in Designer** (the MCP route had failed: `updateObjectSecurity` on that document returns HTTP 500 "Name is insufficiently unique" for any group) and read back as `SO Analysts` direct plus `SO Users` and `SO Administrators` inherited from the folder, so both personas can read it. Verified by render: `SO_analystWatchlist` and `SO_caseDetail` both emit `a!documentImage(document: <logo>)` with **zero** `snowflake-o` fallbacks and no error.
  - **The prior state, kept because it is the reason the switch exists.** MEASURED 2026-09-03, before the grant: the document inherited viewer rights from *another application's* group `FSDFE Users`, neither persona was in it, and `a!documentImage` threw **"User Does Not Have Rights to Perform this Operation"** — which, being an expression error, took the ENTIRE page down rather than showing a broken image. **A design account never sees this**; only a persona login exposes it, so a green design-account render proves nothing about the personas.
  - **DO NOT REMOVE THE FALLBACK.** The document still lives in **Financial Services Data Fabric Examples**, so a deletion, re-import or security reset over there re-breaks every SO screen at once, and flipping this one constant to `false` restores all of them without touching an interface. It is a constant rather than an auto-detect because SAIL cannot ask "can this user read this document" without throwing the very error being avoided.
  - **THE DURABLE FIX IS STILL OPEN, and the standing rule holds: a shipped multi-SC asset must not depend on another application's object.** Upload the PNG into the **SO Artifacts** folder (`8d00e6d4-c983-4df9-ab3b-7f8e998b211f`, which already grants `SO Users` viewer) and repoint `SO_SNOWFLAKE_LOGO` at the copy. Uploading is a **Designer step** — binary documents cannot be created over the Dev MCP (§1). The grant is a workaround; ownership is the fix.
- **The marker cannot go in a grid column header.** `a!gridColumn`'s `label` is a Text-typed parameter; a component placed there validates clean and renders its serialized object graph as the header string (MEASURED 2026-09-03). Marked columns therefore carry a `helpTooltip` and are named once by a logo-marked **legend line directly above the grid**. Repeating the image per cell is explicitly rejected: it would assert that the VALUE is Snowflake intelligence rather than the COLUMN.
- **Provenance display principle — the mark means intelligence, not residence.** The Snowflake mark goes on **Snowflake intelligence**: predictions, scores, and Snowflake-computed aggregates (in the watchlist mockup it sits on `Fail reason` and `p(fail)` — both model output), plus **card-level "live read" source tags** marking a panel read live from Snowflake. It NEVER goes on a value merely because that value lives in Snowflake — trade id, counterparty name and notional are Snowflake-resident and carry no mark. Marking residence dilutes the argument to "our data is over there"; marking intelligence makes the argument "Snowflake is deciding something." Implemented in the mockup as `title="Live from Snowflake"` on a column-header glyph and on a card-level chip.
- Scope is story-governed: features exist only where a beat names Snowflake, the agent, or governance — persona furniture stays small. But everything that ships is built completely and polished; neither UI quality nor the robustness of what's in scope is ever the thing cut.

## Demo Admin site (operator tooling — NEVER a tab on the analyst site)

- **`SO_DemoAdmin` is a SEPARATE SITE** (`settlement-ops-admin`), gated to **`SO Demo Admins`**, a group deliberately **outside the SO Users tree**. An SC running the demo is not a persona, and a reset button one tab away from the watchlist is a wrong click away from deleting the case being demonstrated. Membership is the presenting SC's own account — never `alex.analyst` or `sam.supervisor`.
- **THREE THINGS THE CONSOLE WILL NOT DO, and each is a rule rather than a gap.**
  - **Its readiness check never invokes the agent.** "Agent verified at last run" is read from the most recent machine-written audit entry — evidence the agent HAS run. A readiness check that calls the model costs ~45 seconds, costs money, and writes the very rows it is checking; a check that changes what it checks is not a check. (Loading a feed does start triage — that is the demo, not a check.)
  - **It reaches Snowflake only through the two procedures — updated Phase 5 Part B2, 2026-09-11.** Load starts `SO_simulateRun` (SIMULATE_FEED, then `SO_intakeRun` in the background); Delete starts `SO_resetRun`. No free SQL, no DDL. The procedure's message is shown **verbatim**, OK and REFUSED alike — the refusals are the explanation. The `hero-trade-delete.sql` instruction is retired. A Claude *session* still has no Snowflake execution path; the buttons run as the presenter.
  - **It never loads or deletes the fixture session.** Load and Reset refuse `P4-VERIFY` **by name** — in the console and again in both process models — because "type the session id" is exactly the affordance that makes a typo destructive, and every Phase 4 screen renders from that set.
- **RESET IS APPIAN FIRST, THEN SNOWFLAKE, AND SNOWFLAKE ONLY IF THE APPIAN DELETE SUCCEEDED** (`SO_resetRun`, 2026-09-11). A failure part-way can leave orphan packet rows in Snowflake — harmless, invisible to every screen, cleared by pressing again — but never cases pointing at deleted trades. **Run names are upper-cased on both buttons** so a run's case session tag always matches the upper-cased trade ids; mixed casing is the other way to strand a run across the two systems.
- **A HEALTHY CONSOLE IS QUIET; PROSE IS SPENT ON FAILURE STATES — ratified 2026-09-22 after an SC read v8 as "far too much text, visually poor, overwhelming".** The default healthy view is a one-line verdict ("Ready · data 50,000/50,000 · 9/9 connections · agent OK"), a status line and two buttons with one-line captions — 42 words, down from 312. Every detail lives behind a collapsed **Details** section; only FAILING checks surface on the default view, in red with their detail, and only failing connection names are named at all. Refusals and failures keep full prominence and full verbatim text: those are the states where words earn their place. **Budgets, enforced:** under 60 words of visible prose on the default healthy view, no sentence over 15 words, no value stated twice, no line explaining what another line already shows.
- **DEMO STATE IS NOT A FAULT.** Loaded demo rows are reported neutrally ("Loaded now: 15 demo trades · 3 cases") and never turn the verdict red — v8 flagged the expected mid-demo state in red, which teaches an operator to ignore red exactly as the three-state rule below warns.
- **A READINESS CHECKLIST HAS THREE STATES, NOT TWO.** Green tick, red cross, and a NEUTRAL grey dot for lines that report a state rather than assert one — reserved packet rows present, fixture cases present, comment and event totals. A checklist that paints normal states red teaches its reader to ignore red, and then the one line that IS a failure gets ignored with the rest. Rendered solely by `SO_adminCheck`.
- **Reset deletes CASES ONLY; comments and audit events go by cascade** — `SO Settlement Case` relates to both with CASCADING update behaviour. **MEASURED 2026-09-09**, not assumed: two throwaway cases plus two comments and two events, deleted by case alone, took comments 14→12 and events 16→14 and left the 17 fixture cases untouched.

## Files in this repo

Template-convention files (contracts in the operating core):
- `CLAUDE.md`: this file — operating core, then this build's data model, vocabulary, business rules. Source of truth for generation.
- `BUILD_PLAN.md`: phases and task list — the state of record for what is done and what remains (§10.2). Renamed from `settlement-demo-build-plan.md` on 2026-09-21.
- `BUILD_LOG.md`: what has actually been built, with object UUIDs and decisions; promotion staging and checkpoint (§7, §9). Renamed from `build-log.md` on 2026-09-21; entries before that date cite the old names.
- `TODO.md`: open items (§8); any response that touched it ends with a "TODO changes:" line.
- `Closeout.md`: the last session's full close-out, rewritten whole (§10.1); fetched from GitHub by the claude.ai Project. Renamed from `CLOSEOUT.md` on 2026-09-21 — case matters for the fetch.
- `prompts/`: build prompts, `NNN-<slug>.md`, three-digit run order, exactly as pasted (`GETTING_STARTED.md` §4). Numbering starts at 001 from the retrofit; no earlier prompts were banked in the repo.
- `mockups/`: HTML visual targets, one per Phase 4 interface (watchlist, case detail, supervisor view; the admin/reset mockup is not yet banked).
- `agent/`: SO Triage Agent design — `instructions.md` (deployed v6 verbatim), `tools.md`, `eval.md`, `v6-step3-draft.md` (not applied).
- `GETTING_STARTED.md`, `reference/`, `examples/`, `maintenance/`, `skills/appian-supplemental/SKILL.md`: template-owned, copied unchanged from appian-devmcp-method on 2026-09-21. Edit them in the template, not here; the skill copy is compared with the installed one at preflight (§2.6).
- `.work/`: deployed-source copies and harness generators (template convention).
- `PROJECT_INSTRUCTIONS.md`: the claude.ai Project instructions, generated from `GETTING_STARTED.md` §2 and this build's files. Build-owned and never synced from the template; regenerated only when the build's context changes (`GETTING_STARTED.md` §1, step j).

Project files:
- `pre-settlement-fail-prevention-one-pager.md`: positioning; the talk track's source.
- `hero-trade-spec.md`, `packet-spec.md`: the Phase 5 simulation packet contracts.
- `fixtures/`: the P4-VERIFY fixture CSVs and `p4-verify-redate.py`, the per-session re-date script.
- `snowflake/`: every Snowsight script handed to Scott (setup, procedures, probes, one-grid verification). `hero-trade-delete.sql` is superseded and kept for history.
- `deploy-notes.md`: Snowflake-side deployment record — substitutions, PAT expiry, endpoint, known failure modes. Seed of the Phase 5 SC setup doc.

## Session Closeout

§10 governs, including push verification. This build is where the rewrite-whole rule in §10.1 was bought: it failed twice on 2026-09-09, both times caught by Scott — the failure mode was appending a section to the existing file and calling that a close-out, which left a stale title, a stale object version in a heading, and a file that was a different document from the one printed in the session.
